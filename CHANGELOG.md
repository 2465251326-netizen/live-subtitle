# 更新日志

最新版本说明见 [Releases](https://github.com/2465251326-netizen/live-subtitle/releases)；本文件保留完整历史（v1.6 起）。

### v2.19.3

**全面实测（真实网页英语新闻 + 60 秒合成音频，全程离线 Argos）揪出并修掉流式字幕的"同一句复读"——一条缺陷三个根因**

- **修复·悬浮面板里同一句原文+译文重复出现（行内被拼进三遍 / 同句各占一行）**。真机取证：DW News 直播 150 秒 + 60 秒合成英语新闻各一轮，面板出现 `…now in a One of those people is Lauren, now in her mid-30s. … people is Lauren, now in her mid-30s…` 这种行内复读，以及"膨胀草稿行 + 干净终版行"两行并存。查翻译缓存键证实**送译原文只出现一次**——即复读纯属显示层，识别与翻译侧是干净的。三个根因逐个修：
  - **① 剥离算法漏掉"草稿＝基线整句 + 少量新词"这一最常见形态**：预览通道每 0.9 秒把最近 4 秒音频重转写一遍，草稿天然重复已显示部分。旧剥离要求"基线尾部锚落在草稿前 2/3 内"——27 词基线只多 1 个词时锚落在 ~24 位（限 18）→ 判"无重叠"→ **整句被当成新话返回**（多 9 个词才正常）。现在先做一次"逐词去标点+小写的前缀相等"快判，命中即只返回新词。
  - **② 合并算法只做"已显示后缀 == 新词前缀"**：预览转写常在**句中**就与已显示文本分叉（`now in a` vs `now in her mid-30s`），后缀对不齐就整段追加 → 短语在一行里复读。新增词级回跳去重：草稿开头若干词（≥3，忽略标点/大小写）若已在当前行连续出现，丢掉这段复读只追加新词；3 词护栏保证 `really really good` 这类合法叠词不被误吞。
  - **③ 面板配对只认一个方向**：`_find_pending` 认"草稿是终版的前缀"，缺反方向（**草稿比终版更长**：窗口前瞻把下一句开头也转写进来）。于是终版匹配不到草稿行、另起一行，膨胀版挂着推测译永久留在屏上。补反方向匹配，命中后由终版把行文本校准为权威原文。
  - **另加面板侧兜底**：幕墙末行已收口时，若这一拍草稿仍是"同一句 + ≤3 个新词"直接忽略、更多新词只把新词部分开成行——即使主窗剥离再次失手也不会留下重复行。
- **实测数据（离线 Argos + GPU turbo + 上下双语幕墙，两轮真实音频、环回电平先验证有声）**：
  - 60 秒合成英语新闻：14 段识别 `reco_p50=0.27s / p95=0.48s`；7 句终版离线翻译 `tr_p50=0.10s / p95=0.22s`；感知延迟 `spec_p50=0.16s`；攒句等待 `hold_p50=4.78s`；零异常、零孤儿线程、引擎日志只出现 argos。
  - 网页真实新闻（DW News 直播）120 秒：23 段识别 `reco_p50=0.28s / p95=0.50s`；16 句翻译 `tr_p50=0.08s / p95=0.20s / max=0.48s`；感知延迟 `spec_p50=0.09s`；零异常。修复后面板截帧复核：无行内复读、无重复行、底色实心。
- **文档订正**：v2.19.2 的回归锁实为 **9 把**（单元 +5、集成 +4），当时误记为"8 把 / 集成 +3"。
- **回归锁 +4**：单元 2 把（前缀相等剥离含"新句不得被误吞"反向护栏、词级回跳去重含合法叠词护栏）、集成 2 把（幕墙流式拍不重复已收口句、草稿长于终版时终版就地收口）。套件：单元 **88** / 集成 **122** 全绿。


### v2.19.2

**工作区巡检：四路并行只读审计 + 逐条自己复核，修掉 13 项**（其中 5 项有真机像素、用户日志或反向实验铁证；审计报出的假缺陷另列，未采信）

- **修复·悬浮面板正文区整片透出桌面**（用户实拍："字幕悬浮里面一大片透明的区域"）：根因**不是** v2.19.1 判的"resize 后新区域不重绘"——`QScrollArea` 设了 `setWidgetResizable(True)` 后，Qt 在 `setWidget()` 内部（C++ 侧）把内容控件的 `autoFillBackground` 打开，叠加 `WA_TranslucentBackground` 使它每帧把自己整块矩形**擦成 alpha 0**，连面板 `paintEvent` 刚画好的圆角底色一起抹掉。真机像素实测：修复前正文 = 壁纸蓝 (15,157,250)、100% 像素偏离底色；修复后 = (28,31,38) 且 std=0；反向实验（手动把 `autoFill` 改回 True）立刻复现，单独关 `WA_TranslucentBackground` 或只 `repaint()` 均无效。修法＝四处 `setWidget()` 之后统一收口。
- **修复·卸载语言包后"完成"对话框永不弹**：`remove_pack` 成功后先刷新列表（内部 `lst.clear()` 删掉 QListWidgetItem 的 C++ 对象），之后才去读 `sel.text()` → `RuntimeError` 被 PySide 吞进 stderr，**卸载其实已成功**、用户以为没删掉而反复点。用户日志两次实锤（09-11、09-15）。
- **修复·离线引擎备援后再也切不回来**：`probe_engine` 只有 google/mymemory 两个分支，`argos` 恒返回"未知引擎"→ `engine=argos` + 自动备援（默认开）的用户一次离线异常落到 MyMemory 后，每 60 秒重探主引擎永远失败，**整场被绑在在线引擎上**（额度/429 风险）；而推测式增量翻译要求当前引擎为 argos，随之**静默关闭**（观感＝"译文又变慢了"，日志零线索）。探针语义改为「本方向有可用的本地包」。
- **修复·翻译缓存可能被空数据整份覆写（数据丢失级）**：读缓存时任何异常（记事本改过编码带 BOM/ANSI、杀软或索引器瞬时占用文件）都被吞成"空缓存"，却照常标记"已加载"——绕过 v2.2.1 那道"未加载禁写盘"守卫，首次攒批落盘（10 条或 5 秒）就用近空 dict 覆写整份持久缓存，且不留原件、不打日志。现在：BOM 可读、读失败拒绝写盘、原件另存 `trans_cache.json.bad`、记一条 `translate.cache_unreadable`。
- **修复·多显示器用户的悬浮面板位置每次重启被毁**：配置消毒的"负数无意义"一刀切把 `overlay_x=-1600` 改回默认 200，而副屏在主屏左侧/上方时负坐标本就合法（面板位置夹取逻辑明确支持并落盘）→ 副屏用户每次开软件面板都被拽回主屏左上角。坐标键列为例外，尺寸/条数类键仍拦负。
- **修复·上下双语历史区每句闪无题小窗**：v2.6.6 修过的瞬窗病根（标签无父构造 → 先 `setVisible(True)` 再收编 → 未收编控件建出原生顶层窗口又销毁）在 v2.14.0 新写的 `dual_push_history` 里复现，探针实测每句命中 2 次；旧瞬窗锁只查列表行，查不到这条路径。
- **修复·"100% 不透明"其实常年是 254**：`int(100 * 2.55)` 在浮点下是 254.999… → 254，桌面永远漏 1/255 进来；且面板快捷档/滚轮与重启后 `apply_style` 各写一份公式，同一档位"当场 254、重启 255"两副面孔。换算收口为唯一入口 `opacity_to_alpha/alpha_to_opacity`，主窗不再直接改面板私有属性。
- **修复·经典双语「清空」后三处状态不归零**：① 没清"最近一句"→ 面板已空白但「复制最近一句 / 纠正最近识别 / 纠正译文」仍指向被清掉的句子；② 引导小抄只在列表路径复位 → 经典态每次清空后三行小抄重弹，违反 v2.4.4"每份配置只弹一次"；③ ⋯ 菜单「清空面板字幕」可用态只看列表行 → 经典态恒灰，而工具条「清空」同一动作可用，两入口打架。
- **修复·面板侧改外观键不同步已打开的设置页**：字号 / 透明度 / 布局 / 历史区四项漏网（只有"启用字幕面板"和目标语言有窄同步）。后果不只是显示旧值——用户把控件拨到"屏幕上的实际值"时会被暂存机制判成"改回原值"而**静默吞掉**，显示改了、底部仍提示"所有改动已保存"。
- **修复·首跑向导给没装 CUDA 运行时的机器推荐 large-v3-turbo**：判据只看"驱动能看见卡"，与同文件 `vram_advice` 自相矛盾 → 1.5GB 下载 + 在 CPU 上跑顶级档（每段十几秒），正是 v2.0.11 记过的血泪。补无副作用的运行时就绪判据（CUDA 版 PyTorch 或 nvidia-cublas/cudnn 独立 wheel 二选一），未就绪时推荐 small 并如实说明。
- **修复·幻觉过滤提示在出厂默认下自打脸**：`asr_language=auto`（默认）+ 过滤开启时，状态栏念"当前锁定为「auto」，可在设置中改为自动检测"——用户本来就是自动检测，界面上也没有「auto」这个标签（红线：文案不许与实际不符）。
- **加固·工作线程未捕获异常现在落 app.log**：`sys.excepthook` 只管主线程，采集/识别/翻译/预览线程整条退出时日志零证据，用户观感＝"字幕突然不再出现"而排查只能猜。
- **加固·`bump_version --check` 把 `filevers/prodvers` 元组纳入比较**：旧实现是 `{FileVersion 字符串} or {元组}`，字符串恒存在使 `or` 短路——只改元组也报"版本一致"，产出的 EXE 文件属性却是错版本。
- **工具·单元测试 GBK 噪声根治**：一处 `subprocess.run(text=True)` 缺 `encoding="utf-8"`，跑完整套会打一行 `UnicodeDecodeError: 'gbk' codec …_readerthread`，不影响判定但容易被误读成失败。
- **回归锁 +8**：单元 +5（缓存不被覆写 / argos 探针 / 负坐标 / 透明度量纲 / `--check` 覆盖元组）、集成 +3（正文区底色 alpha 像素锁、双语历史行瞬窗锁、经典双语清空态锁 + 设置页四项窄同步锁）。套件：单元 **86** / 集成 **120** 全绿。瞬窗与底色两把新锁经**旧实现必红**验证（短路修法后只有新锁变红，其余 116 项照旧全绿＝该缺陷此前无任何测试覆盖）。


### v2.19.1

**三轮真机实拍裁决：悬浮面板的句子不再消失（「历史区关」重定义为滚动字幕墙）+ 关攒句后原文译文错配/闪白两修 + 面板半屏透明悬案拆出两个根因**（用户原话从"已翻译的译文和原文会在悬浮字幕里消失"到"句子全部保留，不能在字幕悬浮窗里消失"）

- **变更·「双语面板历史区」语义重定义：关（新默认）＝滚动字幕墙**：dual 布局在关闭历史区时不再走"当前句独占单句槽"，而是**整个面板复用列表行系统**——每句说完留在屏上成对驻留（原文+译文），当前句在最底部随识别实时生长，满屏自动上滚，滚轮上滑回看不被打断，**句子不会从悬浮窗里消失**；开启＝经典上下双语（当前句大字区 + 顶部历史块 + 可拖分割线），原能力零删改。新增 `_classic_dual()` / `_wall()` 两个谓词，22 处 `"dual"` 分支逐一审计改路由；`set_hist_enabled` 两态互清；裸构造仍默认经典态（独立组件与既有测试语义不变）。⋯ 菜单该项改名「经典双语（顶部历史块）」，设置页与 README 文案同步（红线：文案不许与实际不符）。
- **修复·新句草稿原文配上一句终版译文（错配窗口）**：`update_partial` 原先只覆盖原文区，屏上会挂 1~2 拍「B 句草稿 + A 句终版译文」再被草稿译冲掉——即用户看到的"乱跳"。新增 `_dual_cur_open` 状态位做**原子换句**：闭合态且新拍不是屏上句的同源延伸时，原文与译文同刻切换；同源尾部重复只长文本、不动译文态。
- **修复·同一句译文闪白重长**：流式草稿已在屏上生长（含推测译）时，正式片段（大写开头、与草稿同源）晚到被 `_starts_new_sentence` 误判为新句 → 译文区打回 `…` 重新长一遍。现在 `_dual_show_pending` 先过 `_dual_same_sentence`（前缀包含 / 前 3 词同源）：同句就地校准原文、**不重置译文区**，确属新句才重置。
- **修复·已收口句子被迟到的草稿译刷回淡色**：`update_dual_draft_tgt()` 增 `source_text` 配对参数，该句已收口时丢弃迟到的草稿回复（旧单参调用保持兼容）。
- **修复·面板右半屏永远透明**（用户："怎么变透明了"，两个独立根因）：① `WA_TranslucentBackground` 无边框窗口 **resize 后新暴露区域不重绘**——幕墙每来一句 `adjustSize` 长高、启动宽度还会从 309 校正到 619，右半永远停在旧 paint（铁证：右缘三点把手画在旧宽度 295 处，而 `grab()` 后备存储是完整的）。修 `resizeEvent → update()`，修复后右中/下中/左中三点像素全为底色 `#1c1f26`。② 设置页「背景不透明度」滑条量程 0-100 能把值拖到 0 落盘（面板快捷档的地板管不住滑条），重启后直读 0＝全透明。现在滑条量程 30-100 + `apply_style` 再设地板 30 双保险（历史配置里已存在的 0 值也被兜住）。
- **配套·流式草稿行与终版同句收口**：`_find_pending` 补**前缀匹配**（≥8 字符护栏）——流式草稿行是终版前缀时（whisper 终版会补句读）收口为同一行而不是多出一行；`show_pending` 补同句就地校准。此条由新集成锁当场抓出。
- **回归锁**：集成新增 `t_dual_pair_atomic_swap`（**旧实现上必红**，探针 before/after 实证；覆盖错配 / 闪白 / 迟到草稿 / 尾重复 / CPU 成对切换五场景）；`t_overlay_dual_hist_default_off` → `t_overlay_dual_hist_wall_default`（断言 DEFAULTS False + 三句全驻留 + 新句生长行 + 旧行不动 + 经典态回退互清）；`t_overlay_split_follows_mouse` 幕墙分支改断言"无大字区/无分割线"。套件：单元 **81** / 集成 **116** 全绿。
- **取证方法**：真机前台驱动（重开项目 → Chrome 播英语新闻 → 环回 RMS 先验证有声 → UIA 点「开始翻译」→ 每秒抓面板帧），探针脚本 `scripts/qa/dual_disappear_probe.py`、`full_real_drive.py` 留档（gitignore 内，供后续会话复用）。
- **升级提示**：`overlay_dual_hist` 的语义变了但**键名与默认值不变**（默认 False）。此前在 v2.19.0 期间手动改过该键的用户，持久值不会被新版本覆盖——想要字幕墙保持关即可，想要经典大字区+顶部历史块就在设置页或面板 ⋯ 菜单里打开。


### v2.19.0

**用户实拍三改：双语历史区默认关 + 分割线几何修正 + 新增「关攒句」实时翻译开关**（来自真实效果图："历史区域好别扭…分割线我往上拉它却往下…能否关攒句实时翻译"）

- **变更·上下双语面板的历史区改为默认关**（新键 `overlay_dual_hist`，DEFAULTS=False）：实拍实测 619×515 面板里历史区吃掉上半部约 400px，当前句只剩两行字。现在默认由当前句独占面板，且面板**贴内容**——不再撑到 0.68 屏高剩一个空框；关闭时 `dual_push_history` 直接 return（不建控件、不占内存）。能力完整保留：设置页「显示-字幕显示-双语面板历史区」与面板 ⋯ 菜单「显示历史区（上下双语）」均可开回，落盘走 layout 开关同一套路；`apply_overlay_from_config` 顺序契约 layout → hist → src_h_user，保证启动恢复的几何与"一次拖出"的结果一致。**不拆代码**（第二十二节 22.6 与 v2.9.0 的教训：默认关 ≠ 删功能）。
- **修复·分割线"往上拖它却往下"不是手感问题，是几何恒等式错了**：旧分配 `body=贴内容(≤45%总高)`、`hist=总高−工具条−body`，代入得分割线绝对位置 `y = total − sep − 译文高`——**与用户拖的原文高度完全无关**。真实鼠标事件流（`QMouseEvent + sendEvent`，用户同一形态 619×515 + 历史 302px + src_h=87）实测：往上拖 60px → 线 y 反而 **+28**；往下拖 120px → 线 y **−39**；关原文时 body 塌到 46px 地板、钳制区间宽度为 0 → **任何方向零位移**。新分配：历史区份额按内容定（≤45% 屏且 ≤ 可用高度一半），给 body 保底 `body_min=92px`（原文30+把手8+译文30+边距）可拖下限，`body = avail − hist` 与 src 无关 → `y = chrome + hist + src` **一对一跟手**；拖拽与恢复两处钳制上限统一为 `body − sep − 30`（旧值 −24/−40 两处不一致）。
- **新增·「翻译攒句合并」开关 `translate_grouping`**（默认开＝现状两轨制攒句；关＝每片段到达立即送终版翻译，即实时翻译）。关键取舍：分段仍由 `low_latency_mode` / `segment_cap_s` 决定，**与攒句开关解耦**——若直接关低延迟会把切段退回 14s 慢档反而更慢；保存即生效、不重启管线（group=instant，每片送译时实时读配置）。代价按红线如实写进设置页文案：切段处译文不完整、机翻味更重、在线引擎请求量上升（离线包无额度压力）。
- **回归锁 +3、升级 2**：集成新增 `t_overlay_split_follows_mouse`（真实事件流，历史开/关两态各测"先下后上"：方向不反向 + 原文区跟手 + body ≥92 不塌缩 + 松手记录值=实际高度）、`t_overlay_dual_hist_default_off`（默认关不建行 + 打开后能力回来 + DEFAULTS 断言）、`t_translate_grouping_off`（开=两片攒一组只送一次；关=各送一次不进组；低延迟仍为真）；并升级 2 把锁住旧行为的锁（"拉高→历史吃剩余"改为新契约、"dual 终版沉历史"显式开启历史区）。套件：单元 **81** / 集成 **115** 全绿。
- **真机 A/B**（同一 44s 素材、dual + cuda + argos + auto、无占屏）：攒句开＝终版送译 7 次，攒句关＝11 次（逐片即译）；两轮历史区均 0 占位，`当前句区 + 工具条 66px = 面板总高`（188 / 214px）严丝合缝＝当前句独占面板。
- **升级提示**：旧配置里 `overlay_h=515` 的用户，面板会保持 515 高但当前句独占（可拖分割线自由分配）；嫌高点面板 ⋯ 菜单「恢复自动高度」即可。



### v2.18.2

**修复·默认设置下「讲到哪跟到哪」整条通道静默哑火 + 双语面板原文重复上屏 + 首句草稿译文必错一轮**（工作区深度体检实测出的三条，全部有真机铁证）

- **修复·「流式原文」在识别语言=自动检测时永远不出草稿，而且界面零报错**（严重，**出厂默认即中招**）：预览通道把配置里的字符串 `"auto"` 直接当语言喂给 whisper——faster-whisper 只在 `language=None` 时才做自动检测，`"auto"` 会在 Tokenizer 构造处抛 `ValueError: 'auto' is not a valid language code`，而预览线程逐拍 `except` 把它整拍静默吞掉。症状 = 开着「流式原文」（GPU + 上下双语，正是本项目的招牌场景）原文区却只按分段周期蹦句子，界面上没有任何提示，只有日志在刷 `preview.round_failed`。修复 = 构造期把 `auto`/空归一为 None，与正式识别通道（`auto` 时根本不传语言参数）以及翻译层对 `auto` 的既有闸门**保持同一口径**；用户手动锁定语言时照常透传。取证：本机离线模型直跑，修复前 `ValueError`、修复后正常；端到端复测（真 GPU + turbo + 53.5s 英语语音）**原文区在没有新片段到达的情况下逐拍生长 40 次**，`preview.beat` 有节拍、失败日志零条。
- **修复·「上下双语」把同一句原文显示两遍**（中；GPU 用户被流式覆盖掩盖，**CPU 用户直接可见**）：主窗口对**同一个识别片段**调用面板占位两次（方法开头一次 + 建卡之后一次，历史遗留），而双语布局对"小写开头的延续片段"做的是**累加**、无幂等守卫 → 实测原文区出现 `Hello everyone and welcome to the show and welcome to the show`。修复 = 删掉第二次调用，两条分支各自恰好一次；**有意不在面板层加"结尾相同就跳过"的去重**——合法叠词（`非常 非常`）会被误吞，宁可修根因不掩盖。复测（专门关掉流式预览以去掉掩盖源）：13 个片段 ↔ 13 次面板调用、203 个面板快照零重复。
- **修复·首句草稿译文"必错一轮"**（一般）：草稿送译的语言回退链原先只看"本攒句语言 + 配置项"，自动检测下取到 `"auto"` → 翻译层挡成"无源语言" → 离线包抛 `缺少源语言信息…`，而推测式翻译不走备援链、无兜底。真机实测 12 条推测回复里 **4 条带该错误**，用户观感 = 原文在长、译文区不动。修复 = 回退链补"本会话最近一次识别到的语言"，并把 `"auto"` 视同未知：**解不出语言就不送这一拍**（0.9 秒后的下一拍通常已锁定），省一次引擎调用、不留错误态、也不污染推测簿记。
- **加固·整条通道哑火不再无声**：预览连续 3 拍失败时升级记录一条 `preview.degraded`（携带语言与提示）并停止逐拍刷屏——旧行为每拍一条同内容记录，把"功能整体失效"混在噪音里（实测该模式下 2 分钟可刷 130+ 条），现在一行即可定性。
- **回归锁 +3、升级 1**：单元新增 `test_stream_preview_language_auto`（桩模型捕获**真实 kwargs**，断言永不下发 `language="auto"`）、`test_spec_source_lang_resolution`（语言回退链与 auto 排除）；集成新增「每片段仅一次面板占位调用（dual 原文不重复）」「草稿送译拿不到真语言就不送」；**并升级 v2.13.0 的旧锁**——它原先断言"草稿以 auto 送译"，等于把缺陷行为当契约锁住（与 v2.18.1 那把 `objectName` 旧锁同类的教训）。套件：单元 **81** / 集成 **112** 全绿；发版前完成无占屏真机端到端复测（隔离配置 + 数字音频直注）。



### v2.18.1

**修复·面板"只保留一条分割线"其实没做到 + 三个实测缺陷**（用户截图："上一个 agent 没能修好这个界面，我只想要一个分割线"）

- **删掉历史区底缘那条装饰线**（`QScrollArea#PanelDualHist` 的 `border-bottom`）：v2.18.0 号称"只保留一条分割线"，面板上实际仍有两条——这条装饰线 + 原文/译文那条可拖线。两区之间由 6px 布局间距自然分隔，不需要线
- **修复·拖过一次分割线后中央胶囊永久高亮**：`eventFilter` 松手分支只清 `_dual_split_drag` 标志、**漏了把手本体 `set_drag(False)`** → "拖拽中提亮的胶囊"从此常驻，看起来就是第三条又粗又亮的线
- **修复·「原文 关」时那条线仍显示**：`set_show_source()` 漏调 `_sync_dual_visibility()`——关着原文（已无两栏要分）线上却挂着。现在关原文 = 面板无任何可见线（用户裁决）
- **分割线静息态提亮**（alpha 26→70）：多余线删掉后，"保留的那一条"淡到几乎不可辨（真机截图实测仅约 9% 提亮）——等于留了一条看不见的线。现在一眼可见，仍是一根细线，不会回到 v2.16.0 那种 8px 实心灰带"太生硬、难看"
- **修复·dual→list 切换历史区残留**：列表分支只隐了 `_dual_body`，没隐 `_dual_hist`（v2.17.0a 起它在 dual 分支恒显示）→ 切回列表布局残留一整块空白区（实测高 401px）
- **修复·字幕卡聚焦/渐隐样式自 v2.2.5 上线起从未生效**：`set_active()` 改写卡片唯一 objectName，而 `styles.py` 写的是 `QFrame#CaptionCard#CaptionCardActive`——Qt 里 `#A#B` 意为「祖先名 A 且自身名 B」，卡片互为兄弟不是祖孙，永不命中；改名还连带把基础规则 `QFrame#CaptionCard`（卡面底色+边框）一起踩掉。像素实测：聚焦卡渲染成窗口底色 `#0f1115`（卡片"没有脸"），未激活卡才是 `#161a22`。改为**动态属性 `state`** + 属性选择器，并给三个子标签补 unpolish/polish（Qt 不级联重算后代规则，实测历史卡文字极差为 0 即此因）。真机复验：最新句蓝色强调边框 + 历史句文字渐隐均已可见
- **修复·面板 🌐 切目标语言，开着的设置页不同步**：调用点写的是**不存在的** `dlg.reload_values()`，AttributeError 被 `except Exception: pass` 静默吞掉，同步从未发生。新增窄同步 `sync_target_lang()`——不清 `_staged`、不覆盖用户已暂存未保存的该键（不用 `load_from_config()`，那会丢掉其他未保存改动）
- **修复·重跑首启向导会改动用户现有配置**：向导第 1 步恒勾「系统声音」而 `_finish()` 无条件写回 `source_type` → 麦克风用户一路点『完成』就被静默改回系统声音，与设置页"不会改动你的现有配置"承诺直接矛盾（实测坐实：microphone → 走完向导 → system）。现在按配置回显（模型页自 v2.0.3 就这么做，音频源页是漏网的那一处）
- **回归锁 +3**：面板「恰好一条可见分割线」像素扫描锁（含关原文=无线、拖完胶囊复位、切列表不残留）、字幕卡聚焦/渐隐**像素级**锁、向导不改配置 + 面板语言同步锁。并把 `t_card_lifecycle` 里"断言 objectName 字符串"的旧锁升级为断言状态属性——**旧锁把失效机制当成正确行为锁住了**，所以 102 项全绿照样放过一个上线即失效的视觉特性；套件计数见本节末（真实新闻实测又加了 5 把锁）

**真实英语新闻端到端实测揪出的 5 项悬浮窗缺陷**（BBC Global News Podcast 整集，GPU turbo + Argos 离线 + dual 面板，5 轮 × 150s 真机抓屏）

- **修复·流式草稿把同一句上屏两遍**（最刺眼的一条）：真机截图实证原文区出现 "…we can achieve this energy. …we can achieve this energy and our correspondent…"、译文同样重复两遍。根因：每拍只按"上一**终版**句"剥离重叠，没按屏幕上**已显示的当前句**剥离；重叠落在草稿 2/3 之外时锚点搜不到 → 整段被当新话追加（`_merge_stream` 的严格前缀匹配本可救，但 whisper 在窗口尾部会吐省略号 `energy...` → 前缀对齐被打断）。改为剥离基线 = 上一终版句 + 当前已显示全文，并把锚点匹配从"取第一次出现"改为"取最后一次出现"（短锚常用词在草稿前段巧合命中会严重低估剥离量）。**新旧对照实测：旧实现重复 2 次/68 词，新实现 1 次/39 词**
- **修复·关「同时显示原文」时译文区被饿**（正是用户自己的设置）：旧实现只隐藏原文**标签**，QScrollArea 本体仍占 21px、分隔把手仍按可见算 8px、间距按三控件算 24px → 当前句 body 60px 里译文只剩 21px，一句正常译文（22px 字号约 32px 高）显示不全。现在原文关 ⇒ 原文区与把手都不占高
- **修复·dual 原文/译文区内容溢出时不跟底**：长句把最新文字推到可视区之外（实测原文区滚动条 max=49 停在 value=0），用户看不到刚说出的词、必须自己滚轮——流式字幕面板的硬伤。现在两区各自跟底，且**用户上滚回看不被打断**、新句开始恢复跟底
- **修复·推测式翻译污染持久缓存**：真机 150s 会话产生 218 条缓存，其中 39 组是同一句话的渐进变体（最长一句被存 13 个版本）。半句前缀几乎不会再被原样查到（终版文本更长），写进缓存纯属污染：LRU 只有 800 格，真整句缓存被一次性碎片挤掉，磁盘上永久留着大量半截译文。现在推测版**只读不写**；复测同长度会话缓存从 218 条降到 28 条（=28 次终版翻译，0 组重复）
- **修复·历史区写入占位省略号行**：`dual_push_history` 在译文为空时用 `"…"` 顶位，真机历史区出现过只含省略号的行（一次性占位被当成正文永久留存还白占一行高）。现在空译文不写占位，关了原文时该行整条跳过
- **回归锁 +4**：流式草稿不整句重复（可证伪：旧逻辑在同一输入上重复 2 次）、dual 两区溢出自动跟底 + 回看不被打断、关原文时译文区不被饿、历史行不写占位省略号；单元 +1（推测版不写缓存）。套件：单元 **79** / 集成 **110** 全绿



### v2.18.0

**简化·面板只保留一条分割线**（用户反馈："为什么会有三条线"）

- **移除历史区底部的分割把手**（v2.15.x）：历史区/当前句区的高度比例本来就是自动分配（历史吃剩余、当前句贴内容），提供手动分割属于过度设计——三把手并存（历史分割 + 原文/译文分割 + 底缘拉伸）让用户困惑
- **保留的唯一可拖分割线** = 原文与译文之间：上下拖动自由分配原文区/译文区高度（原文区想多大拖多大，译文区让位），比例重启保持（`overlay_dual_src_h`）
- 面板最终结构：工具条 → 历史区（自动高度，滚轮回看）→ 原文区 → **分割把手**（唯一）→ 译文区；底缘拉伸（总高）与右缘拉伸（宽度）不变
- 相关配置键 `overlay_dual_hist_h` 保留兼容旧配置文件但不再读取；相关交互代码（面板兜底命中、三点把手绘制、hit 判定）全部清理
- 回归锁更新：删除历史分割相关锁、真实事件流拖拽锁改为 sep 本体驱动（并修复锁内 processEvents 缺失导致的误报）；单元 78 / 集成 102 全绿

### v2.17.0

**修复·dual 拉高后布局失衡**（用户截图"？？？"：拉高面板后当前句区两三行配巨幅留白、历史区被挤成顶部一小条且裁切）

- **分配重构**：拉高面板的增量改为**历史区吃剩余**（历史内容多、正好多显示）——当前句区贴内容（上限 45% 总高，超长句滚动不再硬撑留白）；分割线拖过的历史比例仍最优先
- **修复·面板总高锁定失效**：`adjustSize` 按 QScrollArea 默认 sizeHint（192px）推导会把面板缩回去（fixed 高度的历史区被裁 76px、拉高"没反应"的元凶）——dual 模式跳过 adjustSize、总高显式锁定为 chrome+历史+当前句+外框
- **修复·历史区恒显示**：无历史行时历史区此前被隐藏，导致拉高面板无处安放空间（同款"弹回"）——现在历史区恒显示（无行时为空白占位区，来句即填）
- 渲染复验（用户"？？？"同场景）：拉高后历史区 488px 占大头（5 句成对完整清晰）、当前句 86px 贴内容、零留白零异常；单元 78 / 集成 103 全绿

### v2.16.2

**修复·dual 原文/译文区白块**（用户实测截图：两区变成两块大白板，文字不可见）

- **根因**：v2.16.0 把原文/译文放进 QScrollArea 时，透明处理只做了 QSS 规则和 viewport 属性的一部分——**真实 Windows 渲染下 QAbstractScrollArea 会用 palette.base（白）填充**，且 viewport 不吃父级 QSS 选择器链，多层防护差一层就露白（v2.4.1 在列表区修过的同款坑第三次重演）
- **修复（三保险）**：① `setFrameShape(NoFrame)` 关闭自带边框；② viewport 透明属性；③ viewport **直接内联 styleSheet**（对自身生效，绕过一切选择器链）
- 真机渲染验证（隔离实例 + 全屏截图 + 播放语音）：无白块、原文/译文/历史区全部深色透明底正常显示；单元 78 / 集成 103 全绿

### v2.16.1

**美化·分割把手重设计**（用户实测："这也太生硬了吧，难看"——指 v2.16.0 的 8px 实心灰带）

- 原文/译文分割把手改为**自绘渐进式视觉**：平时仅一条极淡的 1px 细线（几乎隐形，空态观感干净）；鼠标悬停时细线提亮 + 中央浮现 40×4 圆角小胶囊（可拖暗示）；拖拽中胶囊提亮至最明显——"细线 → 胶囊 → 提亮"三态过渡，不再有突兀色块
- 抓取带高度仍为 8px（命中判定与拖拽体验不变），只是视觉收敛到中心线
- 顺带修复：空态下分隔线误显示的问题（`empty` 属性未初始化导致可见性误判——用户截图圈出的"神秘线"即此）
- 视觉验证：三态渲染对比（空闲干净 / 悬停胶囊浮现 / 拖拽提亮，零生硬色块零裁切）；单元 78 / 集成 103 全绿

### v2.16.0

**dual 原文/译文区重构：各自可滚动 + 分割线自由拖拽**（用户需求："原文和译文不是都应该保留吗，可以用滚轮进行滚上滚下查看"、"原文的显示范围太小了，改成可以自由上下拉长"、"中间的分割线依然不能自由的上下拉长"）

- 原文区与译文区改为**各自独立的可滚动区域**（QScrollArea）：长句超出当前高度时不再被裁切，**滚轮上下即可看全**——原文/译文内容完整保留
- 原文与译文之间的分隔线升级为**可拖分割把手**（三点把手 + 上下箭头光标）：上下拖动自由分配原文区/译文区高度——原文区想多大拖多大，译文区吃剩余（两者各自滚动不怕挤没）
- 拖出的比例重启保持（`overlay_dual_src_h` 配置键，恢复默认归零回自动贴内容）
- 真实事件流验证：`QMouseEvent + sendEvent` 驱动分割线处实际接收控件 → 事件过滤器，拖动分配实时生效

### v2.15.3

**修复·分割线拖拽在"布局缝隙"处失效**（用户实测 v2.15.2 仍拖不动；与历史区/当前句区之间那条线相关的最终一处）

- **根因**：分割线（历史区底边）恰好落在历史区与当前句区之间的**布局缝隙**（spacing 6px，无任何子控件）——鼠标按下时 `widgetAt` 返回**面板自身**、事件直达面板的 `mousePressEvent`，而该命中分支在 v2.15.2 重构时被误删（当时认为事件过滤器已全覆盖），缝隙路径彻底无主 → 拖拽不启动。事件过滤器路径（点在子控件上时）本身工作正常
- **修复**：恢复面板 `mousePressEvent/mouseMoveEvent` 的分割命中兜底分支（全局坐标、与过滤器同一命中中心），与事件过滤器形成**双通道双保险**——点在子控件上走过滤器、点在缝隙走面板兜底，两条路径共用同一套拖拽应用逻辑
- 端到端实测：缝隙处按下 → 拖 +30/+80 历史区 86→136 精确跟手 → 松手收口 ✓
- 回归锁保持（真实事件流拖拽锁 + 全局坐标命中锁均通过）；单元 78 / 集成 103 全绿

### v2.15.2

**修复·分割线拖拽事件路由重构**（用户实测 v2.15.1"还是不行"——坐标修复方向正确但覆盖不全）

- **根因补全**：v2.15.1 修了坐标错位，但还有两个 Qt 事件模型事实没绕开：① mouse **move/hover 事件在子控件忽略后不会传播给父控件**（只有 press 会重新投递）→ 悬停光标反馈失效、拖动中的 move 也可能收不到；② 命中带上半落在 QScrollArea 内，**press 会被它直接消费**
- **重构**：分割线交互全部改走**事件过滤器**（挂在历史区、滚动区视口、历史容器、当前句区、原文/分隔/译文标签共 7 个子控件上）——过滤器在子控件层面拦截 press/move/release/hover，坐标一律取 `globalPosition`（传播/过滤事件的 position 相对原接收者、不重映射）。悬停分割线现在会正确显示上下箭头光标（逐子控件动态设置）
- 真实事件流验证：`QMouseEvent + sendEvent` 按真实路径（分割点处实际接收的子控件 → 过滤器）完整驱动按下/拖动/松手——历史区 56→76→106→**136 精确跟手**、松手收口
- 回归锁 +1：真实事件流拖拽全链路（press 启动 / move 跟手 / release 收口）；单元 78 / 集成 102→103 全绿

### v2.15.1

**修复·分割线拖拽命中判定坐标错位**（用户实测 v2.15.0"还是不行"）

- **根因**：分割命中判定用 `event.position()`（相对坐标）与历史区底边比对，但鼠标事件从子控件（QLabel/QScrollArea）**传播到面板时坐标不重映射**（仍相对原接收者）——判定拿到的 y 天生错位几十至几百像素，±9px 命中带永远打不中（独立脚本实证：局部坐标恒 False、全局坐标全命中）。修正：命中与光标反馈全部改用**全局屏幕坐标**比对
- **命中带优化**：中心下移 2px、宽度 ±9px——分割线上半落在 QScrollArea 内（press 会被它消费），可用的主要是下方一侧，带心偏下让有效命中区最大化
- **修复·自动高度模式单行历史被隐藏**：与 v2.14.1 同源的旧 40px 显示门槛在"自动高度"分支漏改，单行历史（内容需求 ~38px）整条隐藏 → 分割把手无处显示。补齐为与锁定总高分支一致的 56px 保底
- 回归锁 +1：全局坐标命中断言（底边/±边带/历史区内部不误判——并钉死"局部坐标恒 False"这一旧缺陷行为）；单元 78 / 集成 101→102 全绿

### v2.15.0

**新增·dual 分割线自由拖拽**（用户需求："中间的分割线依然不能自由的上下拉长"）

- 历史区与当前句区之间的分割线从**纯装饰**变为**可拖把手**：鼠标移到分割线（历史区底边中央，带三点把手提示）出现上下箭头光标，按住上下拖动即可自由分配两个区域的高度——上移历史区变小（原文区变大）、下移历史区变大（回看更多历史）
- 拖出的比例**重启保持**（`overlay_dual_hist_h` 配置，恢复默认归零回自动分配）
- 两种高度模式下都生效：面板总高锁定时（拖过底缘）拖分割线是两区间的零和分配；自动高度模式下拖大历史区面板随之长高
- 回归锁 +1：拖大/拖回两方向的高度分配断言（锁定总高模式）；单元 78 / 集成 100→101 全绿；渲染对比验证（交界 43%→70%、零裁切零重叠）

### v2.14.1

**修复·dual 面板底缘拉伸完全失效**（用户实测："完全拉不了，连最下边的边都拉不了"）

- **根因一（高度分配）**：v2.14.0 把拉高增量全分给历史区，历史无行时 `hist_h` 强制 0、当前句区固定内容高 → 拖底缘面板**弹回原高**，观感即"完全拉不动"。修正语义：**拉高 = 锁定面板总高**——当前句原文区吃"总高-工具条-历史"的全部剩余（拖动即时反馈），历史区最多占 55%、保底 56px 可滚；历史无行时当前句区独占整板
- **根因二（历史区被门槛杀掉）**：单行历史的内容需求高约 38px，被"内容 ≥40px 才显示"的门槛整条判死（集成锁与独立调试双双实证）。修正：有历史行即保底 56px（可滚）
- **可视化**：底缘新增横向三点把手（此前右缘有 ⋮ 而底缘是隐形手势，"找不到拉伸手柄"的观感来源之一），拖拽中把手提亮反馈
- 回归锁 +1：拉高后原文区必须真实变大、总高锁定、历史出现不被压瘪；单元 78 / 集成 99→100 全绿

### v2.14.0

**dual 布局历史区 + 面板自由拉高**（用户需求："原文和译文不是都应该保留吗，可以用滚轮进行滚上滚下查看；原文的显示范围太小了，改成可以自由上下拉长"）

- **新增 dual 历史区**：整句终版翻译到达后自动沉入历史（原文小灰字 + 译文小白字成对行），**滚轮上下回看**；当前句大字区继续流式实时（草稿→终版→沉历史→下一句的循环）。历史区有内容才显示（无历史时当前句区独占），上限 30 行防无限增长
- **滚轮跟随语义**（与列表模式一致）：新历史行到达时自动滚底跟随；用户上滚回看时不打断，滚回底部恢复跟随
- **面板自由拉高**：底缘拖拽（或设置面板高度）拉高面板后，**历史区显示范围随之变大**（此前 dual 拉高只撑当前句区）；总高上限从 0.55 屏提升到 0.68 屏，当前句大字区另设 300px 上限防止超长句独占整板
- 长句溢出保护：当前句区内容超出高度时不再静默裁切（配合拉高与上限策略）；「同时显示原文」关闭时历史行的原文行一并隐藏
- 清空按钮语义扩展：同时清空历史区与当前句区；收起态/精简条/字号调整均兼容历史行
- 回归锁更新：dual 流式接线断言升级（终版沉历史 hist_rows、当前句数据清空、非运行态忽略）；单元 78 / 集成 99 全绿；面板本体渲染三轮验证（6 句历史成对完整可见、拉高后历史区变大、当前句草稿态、零裁切零重叠）

### v2.13.0

**译文实时化 + 流式通道三修**（用户实测反馈"译文还那种攒句，原文也有攒句感"）

- **草稿也送推测翻译**（译文实时化的关键）：流式草稿此前不送译，译文只在正式片段到达（分段周期 2.5~4s）才刷新——现在每拍草稿（~0.9s）即送离线引擎推测翻译，**译文区与原文区同节奏实时生长**。真机密集拍实测：译文 **1.8 秒即出现**且与半截原文语义对应（旧版 4.2s 才一次性整句出现）。仅离线引擎生效（argos 0.06s/次无额度，0.9s 一拍毫无压力；在线引擎请求量 ×4.4 保持不送）；草稿译文回复按"最新草稿全文"一次性配对，迟到草稿自动作废，整句终版与片段推测版照常覆盖
- **修复·重叠剥离整句重复**：正式识别与预览是同一段音频的两次转写（标点/大小写/个别词必有差异），原严格字符串对齐从首字符就失配 → 剥离失败 → 首片到达后原文区**整句重复上屏**（真机截图实证）。改用词级锚点对齐（base 尾部 5→1 词，lower+去尾标点容错；CJK 源用字符锚），大小写抖动也直接容错；锚全失配时返回全量（宁可少量重复下一拍自愈，不丢新话）
- **修复·草稿翻译语言回退**：首片到达前 `_tgroup_lang` 为空串，argos 找不到 `""→zh` 的语言包直接抛错（spec 不走备援链）→ 草稿译文全灭（真机密集拍实证：译文 4.2s 才随首片出现）。语言回退链 `_tgroup_lang → asr_language 配置`
- **修复·中途切布局流式断粮**：tap 与预览通道此前在 start_pipeline 时按布局一次性判定，运行中从列表切到 dual 流式永远不启动（用户"原文攒句感"的来源之一）。现在 tap 常开（无接收者的 emit 成本 µs 级）、通道创建与布局解耦、`_on_panel_layout_changed` / 设置页保存后按闸门**热启动/暂停**（stop→restart 重置停止标志与陈旧音频缓冲）
- **新增预览节拍遥测**：每 20 拍记录 `preview.beat`（实际间隔均值 vs 0.9s、推理耗时均值）——GPU 分时排队是否拖慢预览节奏，日志一行可辨
- **附·用户配置实测提醒**：`neural_vad`（实验开关，v2.9.0 默认关）在真实带背景音素材上实测句末判停反而更黏（详见 v2.9.0 实测数据），会拉长切段节奏——若感觉识别出字慢，建议在设置里关闭该项对比
- 回归锁 +3：草稿送译与迟到草稿作废、流式预览热重启（stop→restart 重置缓冲）、重叠剥离升级用例（大小写抖动容错）；单元 77→78、集成 98→99 全绿

### v2.12.0

**新增·dual 布局流式原文通道**（用户需求×3："主持人讲了什么英文，必须实时显示在原文里，必须实时不能停"）

- **滑动窗口流式识别**：每 0.9 秒把最近 4 秒音频用 whisper 重识别一次（beam=1 最快档，与正式识别共享同一模型实例、GPU 分时复用），窗口转写文本与已确认文本做尾部重叠剥离后，把新增话音实时追加到原文区——连续语流下原文延迟从"分段周期 4 秒"压到约 1 秒，讲到哪跟到哪
- **实测铁证**（连续语音密集连拍 2.0/3.0/4.0/5.5 秒）：播到 2.0s 时原文区已显示大半句（旧版此时必然空白——首片要攒满 4s 分段才出）；3.0s 窗口尾部带部分转写草稿、逐拍自我修正；4.0s 收敛为完整正确整句、译文随即跟上
- **译文节奏不变**：草稿不送译（每 0.9s 改写一次不值得）；译文仍由分段正式识别 + 推测式增量翻译负责（spec_p50≈0.1s）
- **启用闸门**：`stream_preview` 开关（默认开）×「上下双语」布局 × 识别计算方式 cuda——预览每 0.9s 重识别一次，CPU 上单次要数秒、反而拖垮正式识别，故 CPU 模式自动停用；GPU 下与正式识别分时共享模型（CTranslate2 推理线程安全、模型只读），实测单片识别耗时反而从 0.52s 降到 0.28s
- 实现：新增 `app/asr/preview.py` StreamPreview 线程（滑动窗口 + 最快档转写 + 周期对齐跳拍）；CaptureThread 原始音频旁路 `raw_chunk`（90ms 聚合、仅流式开启时发射）；主窗尾部重叠剥离对齐 + 终版收口基线接力；设置页新增「流式原文（上下双语增强）」开关
- 回归锁 +4：重叠剥离（含转写抖动场景）、启用闸门（开关/CPU/布局三重）、dual 草稿上屏与终版覆盖、主窗对齐链路（基线生长/剥离追加/收口接力/停止忽略）；单元 75→77、集成 96→98 全绿

### v2.11.0

**新增·悬浮面板「上下双语」布局**（用户需求："不是那种一个框一个框里有原文和翻译，而是上半部分是原文，下半部分是译文，UI 要好看一点"——对标豆包 PC 实时翻译）

- **新增 `overlay_layout` 面板布局设置**（设置 → 显示-字幕显示，保存即时生效；面板 ⋯ 菜单可随时互切）：
  - 「列表历史」= 原有形态：原文+译文成对的历史滚动区（默认，行为不变）
  - 「上下双语」= 豆包风当前句聚焦：**上半是随识别流式生长的原文**（淡色小字：新句重置、延续片段打字机式拼接），**下半是加粗大字译文**——配合推测式增量翻译，原文一出译文立即就地更新、整句终版收口，说完即换下一句，不保留历史（历史仍在主窗口与导出里）
- **视觉层次**：原文约主字号 78% 淡灰 + 细分隔线，译文主字号加粗主色；「同时显示原文」关闭时自动切为纯译文大字模式；收起态迷你条、字号/颜色/透明度/置顶/拖动/贴边全部复用既有面板能力
- **修复·自 v2.4.3 起的高度排期状态机缺陷**（dual 模式逼出来的历史 bug）：`_consume_relayout` 收敛后 `_relayout_pending` 永久残留 True，之后所有重排排期被守卫吞掉——列表模式因有滚动条兜底视觉无感（历史一直未暴露），dual 模式无滚动兜底，第二句起高度永远停在首句值、长终版底部裁切（真机截图+探针轨迹实证：want=124px 而 body=74px、pending 恒 True）。修复后两种布局的高度都真正逐句跟随，列表模式"高度贴内容"也随之更及时
- **修复·dual 空态占位**：经配置切到 dual 的实例（构造后的正常路径）空面板一片空白——set_layout_mode 切换即补空态占位（首次使用升级手势引导的语义保留）
- **dual 字号/字重改用 setFont 落地**：QSS 的 font-size 不会写回 widget.font()，QLabel 的 heightForWidth 按默认字体度量，长句需求高度被算小而裁切；高度计算同步改用 heightForWidth 真实换行高度
- 回归锁 +3：dual 全链路（流式生长/推测就地更新/终版收口/清空/纯译文显隐/收起态）、布局配置恢复与菜单切换落盘、**多句高度跟随（pending 释放）**；集成 93→96、单元 75 全绿

### v2.10.0

**悬浮字幕面板启动即常驻**（用户需求："打开软件，字幕悬浮窗一直常驻，按热键来显示或隐藏"）

- **启动即常驻**：软件一打开悬浮面板必定显示（不再读上次退出时的隐藏状态），实时字幕随开随见，无需再手动唤出；「隐藏」（热键 / 面板 ✕ / 设置页取消勾选）只在**本次运行内**有效，下次启动自动恢复常驻
- **隐藏仍然被全程尊重**：运行中隐藏后，本次开始/停止翻译都**不会**强制重新弹出，直到你按热键唤回或下次启动
- `overlay_enabled` 配置语义随之更新为"当前是否显示"的运行态：启动必显示并落盘 True（设置页勾选、仪表盘「字幕面板：已开启」、托盘菜单状态全部如实一致）
- 修复速览卡取词顺序：先显示面板、再刷仪表盘文案（旧顺序可能让仪表盘谎报"已关闭"）
- 新增回归锁：上次落盘 False → 构造后仍常驻且回写 True → 热键隐藏落盘 False → 再按恢复；集成套件 92→93 全绿
- 顺带修复 README 与 v2.4.0/v2.5.0 脱节的遗留段落：特性亮点"三形态悬浮条"改为字幕面板实况、删除早已退役的「描边」「右键关闭悬浮字幕」「不透明度 0-95%」描述、托盘右键菜单列表补齐实况六项

### v2.9.0

**用户复评恢复方案 B**：神经 VAD 句末判定（实验开关，**默认关**）

- v2.8.0 曾将其回退（实测零收益）；用户要求保留该能力以备试验，本版完整恢复为设置开关 `neural_vad`（设置 → 语音识别-语言与计算，**默认关闭**）：开启后采集期间由 Silero 神经模型替代能量判据判定"这块是否人声"，可在能量判据被突发强背景乐/噪声骗住时按真实停顿切句；模型缺失或加载失败自动静默退回能量判据；开销约 0.4% CPU（每 32ms 音频块推理 0.14ms）
- **默认保持关闭的原因如实写入设置页文案**：实测纯净素材它与能量判据持平（15 句切 15 段、零硬切），带稳定背景乐的素材它反而句末判停晚约 0.7 秒（滞回阈值 0.50 进/0.35 出会多抱一段尾音）；能量判据的自适应噪声底本就压得住稳定背景乐。仅在"突发强背景乐盖过语音、字幕明显不切句"时建议开启并前后对比字幕节奏
- 默认路径（开关关闭）与历代版本行为逐字一致；实测结论与调参注意事项落档于分段器代码注释，防止未来误诊重做
- 测试：单元 73→75（恢复神经判定注入、滞回与异常降级两锁，新增「neural_vad 默认必须为关」锁——要改默认值先拿新实测数据）；集成 92 全绿

### v2.8.0

**译文速度专项**（用户反馈"翻译速度还是太慢"——先用日志遥测定位瓶颈，再真机 A/B 验证）

瓶颈定性：真机会话遥测显示识别中位 0.55 秒、翻译中位 0.06 秒，两者合计只占端到端延迟的
13%；真正的大头是**攒句等待**（中位 4.13 秒，连续语流复现 5.04 秒），即"上屏碎、翻译整句"
两轨制下，本句译文必须等下一片到达才被冲刷出去——而下一片到达间隔恰好等于分段上限。

- **新增·推测式增量翻译**（设置-语音识别，默认开）：识别碎片一到达就先翻译"目前攒到的文本"
  并立刻上屏，下一片到达再送更长版本，译文在同一张字幕卡／同一面板行上**原地生长覆盖**；
  整句攒完后终版译文接管（终态、计入会话条数、更新时间轴）。真机 A/B（连续语流，
  同素材 / 同模型 / 同 GPU 两轮对照）：译文感知延迟 **5.13 秒 → 0.10 秒（约 51 倍）**，
  识别侧段数与耗时完全一致、零副作用
- **限定·只对离线引擎推测**：Argos 离线包无额度限制、单次翻译仅 0.06 秒，逐片加发中间版
  毫无压力；**在线引擎（Google / MyMemory）自动保持整句翻译**——它们有额度与限流
  （MyMemory 每天约 5000 字符免费额度），逐片加发会让请求量成倍增长。引擎自动探测（auto）
  按探测后实际选用的引擎判定，离线才推测
- **新增·连续语流分段上限可调**（设置-语音识别，默认 4 秒 = 榨干模式档）：2.5 / 3 / 4 / 6 /
  10 秒或「跟随模式默认」六档。实测译文等待中位数约等于该值，调小则字幕刷新更密，代价是
  句子更易被腰斩：真实素材实测 2.5 秒档有 57~71% 的句子在上限处被硬切，4 秒档只有 0~17%，
  因此激进档保留为可选项而非默认
- **改进·停止时保留推测译文**：停止翻译时若某句只有推测到的半句译文，**保留该译文**并标注
  "译文可能不完整"，不再覆盖成"未完成翻译"的失败文案（半句也比没有强）；这类卡片与其他
  失败卡同过滤规则，不会混入 TXT / SRT 导出之外
- **修复·推测链路的三重防护**：① 翻译队列满（≥5）时推测提交**放弃自己、绝不挤掉终版**
  ——被挤掉的终版文本不会再来第二次，对应卡片会永久悬挂 "⟳ …"；② 推测失败**不触发引擎
  备援链**（备援会改写当前引擎，导致"中间版把引擎切走、终版用了新引擎"的漂移，且中间版
  几秒内就被覆盖，为它跑一遍降级重试纯属浪费）；③ 组已冲刷后的迟到中间版按代数守卫一律
  丢弃，不会盖掉已终态化的卡片
- **说明·延迟遥测新增 spec 段**：日志 `pipeline.latency` 增加 `n_spec / spec_p50 / spec_p95`
  （碎片到达 → 推测译文上屏，即用户实际感知的译文延迟），`hold_*` 照旧记录整句终版的攒句
  等待以便对照
- **说明·本项目实测否决了"换神经 VAD 切句"方案**（曾完整实现后回退）：`攒句等待 ≈ 分段上限`
  的真因是**句长超过上限被强制切段**，而不是"找不到句末停顿"。短句素材（15 句 × 1.5 秒、
  句间 0.95 秒静音）叠 rms 0.03 的持续背景乐后，能量判据仍是 15 句切 15 段、零硬切——
  自适应噪声底（阈值 = max(噪声底×3, 0.004)，噪声底上限 0.02 → 阈值最高 0.06）本就压得住
  稳定背景乐；而 Silero 神经判定（滞回 0.50 进 / 0.35 出）在同一素材上反而多抱 0.66 秒
  尾音与背景乐（段长中位 2.16 秒 vs 1.50 秒）。神经 VAD 开销虽仅 0.4% CPU，但零收益
  且在采集线程加载 onnx 徒增风险面，故不留该开关。此结论已固化为单元测试防日后误诊

### v2.7.5

**修复**（v2.7.4 后新一轮潜在 BUG 与设置逻辑审查，8 项一次结清）

- **修复·退出程序时本会话新增的翻译缓存可能丢失**：翻译线程被强制终止前，主线程代为落盘一次（缓存写入自带锁且为原子替换，与后台线程并发安全）——挂机多时的积累不再随退出硬杀丢掉
- **修复·"语言复检"丢弃段静默消失**：复检发现语言切换但置信度不足时整段保守丢弃，此前面板毫无痕迹（"字幕无预警跳过一大段"）——现在留一张弱化提示卡说明丢弃原因；此类卡与失败卡同过滤规则，不混入 TXT/SRT 导出
- **修复·字幕上屏后"信号过弱"告警标志未复位**（v2.6.2 重构半迁移：横幅撤了、标志位还在，下轮静音期判定仍引用旧状态）
- **修复·手工编辑配置文件写负数（如窗口尺寸/历史条数）**：此前被原样采纳可能导致显示异常，现在负数一律回默认值
- **改进·离线翻译包预载定向**：识别语言锁定时只预载该方向、自动检测只预载最常用的英→中（此前无差别预载全部已装方向，白占内存与启动时间）；其余方向仍在首次真实翻译时按需加载
- **改进·拆除 v2.7.0 死管道**：字幕信号中的"段尾静音/末片置信度"两个参数经 v2.7.4 审计证伪（whisper 将末片结束时间拉伸补齐到音频尾，判据退化为纯时间地板）后已无消费方，本轮整体拆除，信号接口回 4 参
- **说明·"榨干模式"补生效时机**：GPU 量化/进程优先级/切段上限为重启翻译生效；冲句节奏为保存后即时生效
- **文档·v2.7.4 条目"恢复默认不再漏网"措辞澄清**（内部键本就跳过恢复默认，行为一直正确）

### v2.7.4

**修复**（地毯式活体+全库静态审计批次，15 项一次结清）

- **修复·搬移数据目录后设置可能被旧快照回滚**：启动"指针"从一次性全量快照改为每次保存同步、且只存目录一项——此前迁移后改的设置重启会退回旧值，极端情况重新迁移还会用旧快照覆盖新目录
- **修复·手工编辑配置文件写错格式导致启动即崩**：所有设置项读取时逐键消毒（如字号误写 "18px" 回退默认而不是崩溃；数字字符串"200"仍会正确采纳）
- **修复·退出程序时最后一句翻译被硬杀**：退出路径现在也通知翻译线程"关门收工"再等它自然退出（此前每次退出强杀，还可能丢一次缓存落盘）
- **修复·末句"识别中"卡片永久悬挂或重复出现**：翻译线程已停止时残句立即终态化；识别线程退场时把攒句缓冲强制送出
- **修复·TXT 导出混入"⟳ …"占位与失败卡**（与 SRT 出口统一过滤）
- **修复·连点两次"检测 GPU 环境"或"CUDA 安装"可能崩溃**：后台任务进行中再点不再顶掉旧任务
- **修复·设置窗口有未保存改动时从面板菜单再次打开，改动被静默清空还提示"已保存"**：现在只把窗口带到前台，内容原样保留
- **修复·面板上把不透明度调到 95% 以上，进设置页一保存被悄悄压回 95%**：设置页滑条量程统一为 0-100
- **修复·速览卡"按 Ctrl+Alt+O 打开"是硬编码**：改按真实按键设置+注册结果说话，未生效就写"未生效"
- **修复·首次向导教旧文案**（"启用字幕面板（置顶）"早已改名，置顶语义归面板 📌）
- **修复·两处设置说明互相矛盾**（"语言复检"与"自动检测"文案对齐，复检丢弃语义如实写明）
- **修复·攒句整句翻译时面板行原文只有半句**（与主窗卡片统一为完整整句）
- **修复·卡片右键"加入纠错词典"要等下次开始翻译才生效**：与设置页同一即时热更路径
- **修复·自动模式所有在线通道失败后永不恢复 Google**：重探目标固定为 Google 本身而非卡死的备援
- **改进·五个面板运行态设置键补登记**（「恢复默认」以前漏掉它们——现已登记并能在恢复默认时一并还原，加双向自动断言防复发）；模型下载进度扫描 600ms→1.5s 减少下载期磁盘空转；清理一处死代码与一处重复信号连接

### v2.7.3

**修复**（挂起案销账：每次停止翻译必现的"孤儿线程"）

- **修复·停止翻译后总有一个后台线程滞留 15 秒**：翻译线程为等识别尾句的转发，设计上会空等 15 秒排水宽限，而停止流程只等 2.5 秒——于是每次停止都被记成"孤儿线程"，期间白占资源。现在识别线程转发完最后一句即通知翻译线程"关门"，队列排空秒退（实测停止后 <1s 退出，日志改为如实的 draining 标签，孤儿不再出现）；尾句译文不受影响（排水语义原样保留）

### v2.7.2

**新增**（用户反馈：识别/翻译还想更快，希望榨干硬件）

- **新增·榨干模式**（设置-语音识别，默认关=一切照旧）：一个开关同时做四件事——
  ① GPU 推理权重压缩为 INT8：显存占用约减半（6GB 卡边看视频边翻译更从容），识别耗时小幅下降（实测中位 0.44→0.41s；识别率可能轻微下降，介意请保持关闭）；
  ② 翻译运行期进程优先级提升：采集/识别线程在系统忙时不被抢时间片（后台其他程序相对变慢）；
  ③ 连续说话不断句时的强制切段上限 6s→4s：实测 25 秒连续语流交付 3 段→4 段，最后一块内容更早出来（代价=句子更易被腰斩）；
  ④ 提前冲句地板 2.0s→1.2s：说完即冲更快（代价=句中长停顿更容易被当成句尾）。
- **说明**：本机实测 INT8 在短片段上提速有限（<10%），主要收益是显存与交付节奏；模型加载会多 ~1 秒（量化转换），由启动预热吸收，用户无感

### v2.7.1

**新增**（用户反馈：翻译引擎应该可以关掉自动切换）

- **新增·引擎自动切换开关**（设置-翻译，默认开=旧行为）：此前引擎失败时永远自动降级备援（在线互备→离线包）、恢复后悄悄切回。现在可以关掉——关闭后固定使用你选的引擎，失败只在字幕卡与状态栏明确报错，绝不换通道；适合要求译文来源绝对稳定的场景（对照测试、离线包用户）

### v2.7.0

**速度与准确**（识别+翻译链路优化批：全链路真机实测，含 A/B 对照）

- **修复·悬浮面板偶发"译文配错原文"**：下一句字幕先到、上一句译文迟到时，迟到译文会填进新句子的行（面板旧设计只留一个"待决槽"，会被后来的句子覆盖）。现多句待决并存、译文按原文精确配对，攒句合并的前片行同步收编
- **新增·提前冲句**（默认开，可关）：说完最后一句后的整句译文等待从 3.5 秒缩短到 2 秒——实测（合成语音 2 秒句距场景）攒句等待中位 **6.0 秒→1.5 秒**；连续说话节奏不变（译文仍按整句成批出）。设置-语音识别可关
- **新增·热词提示**（设置-语音识别，默认空=关闭）：把常出现的人名/地名/产品名预先提示给识别模型，专名识别有改善可能——真机 A/B：无提示时自造专名被错听成 Thragedem/Veselon，加提示后 3/3 全对；文案如实不保证
- **新增·语言复检**（默认开）：自动检测下每 20 段临时解除语言锁定重听一次——中途换语言的视频或开头锁错时能自动纠正；此前语言一旦锁错整场坏
- **改进·离线翻译首句不再卡**：语言包改为选定引擎后后台预载；此前首句翻译现场加载模型数秒，期间队列塞满可能丢早期字幕
- **改进·启动预热补完最后一公里**：预热线程现在会跑一次空转写，"开始翻译"的第一句不再比后续慢 1~3 秒
- **改进·在线翻译跟随识别语言**：短句/歧句不再被翻译服务逐句重新猜语种（"Cheers." 猜成德语这类），与识别侧语言复检配套
- **改进·攒句延迟首次可见**：日志新增 hold 遥测（pipeline.latency 的 hold_p50/p95），"说完→整句送去翻译"的等待时长一行日志即查

### v2.6.6

**修复**（用户实测反馈：翻译时高频闪现小窗口，来不及看清就消失）

- **修复·每来一句字幕就闪一个 40 毫秒的小窗**：面板行卡片化（v2.5.0）遗留的隐患——原文标签在挂入面板之前是"没有归属"的独立窗口状态，"同时显示原文"开启时每句字幕都会先把它以独立小窗弹出、再收编进面板销毁，即用户看到的闪窗。现改为构造即归属面板、可见性切换挪到收编之后；真实平台整场翻译实测闪窗 19 次→0 次，并新增集成回归锁（82 项全绿）

### v2.6.5

**改进**（全面体验审查：UI 全量走查，offscreen 截图 + 交互实测）

- **修复·设置页开关看不出归属**：勾选框此前被推到行最右，悬在下方说明文字的右上角、与标题分离，看起来像装饰点；现紧贴标题排布（显示/翻译/通用页全部开关行）
- **修复·设置页滚动条是刺眼的亮色原生样式**：设置对话框的独立样式表覆盖了全局滚动条规则，回退成原生亮色滚动条压在内容右缘；现恢复全局 8px 细暗色样式
- **改进·设置页滚动到底不憋闷**：内容与底部操作栏的间距加大（26→48px），最后一行文字不再贴着分隔线被生硬截断
- **修复·悬浮条图标按钮全靠猜**：语言选择、原文/译文切换、字号、↓最新、⋯、收起、✕ 共 8 个按钮补齐悬停提示（此前只有 2 个有）
- **改进·主窗状态点含义可悬停查看**：红/黄/绿点现在有悬停提示（未启动/运行中），状态文字被截断时也能知道含义
- **改进·没有字幕时「清空」「导出」置灰**：此前可点、点了弹窗说"没有可导出的字幕"；现在无字幕时禁用，出现第一条字幕自动恢复

### v2.6.4

**改进**（全面代码审计第四批：性能与韧性）

- **修复·启动预热与「开始翻译」并发时双份加载模型**：两者同时构造模型（GPU 冷初始化最长 49 秒×2、显存/内存峰值翻倍）。现加载互斥：真实加载等预热完成直接命中复用池秒就绪，预热在真实加载进行中时自动让位
- **改进·翻译缓存跨引擎共享**：缓存键不再包含引擎名——此前备援切换/主引擎恢复后旧键永不命中，缓存被稀释白占；现在同句译文跨引擎复用，命中率不再被引擎切换稀释
- **改进·网络请求连接复用**：每条字幕的翻译请求复用进程级 HTTP 连接池，省去每次新建 TCP+TLS 握手的 1~3 个往返延迟
- **修复·音频设备短暂抖动不再中断翻译**：蓝牙耳机休眠唤醒、USB 瞬断、设备热插拔瞬间会触发一次读取失败，此前直接停整场翻译需手动重开；现在 1.5 秒内自动重试自愈，连续失败才报错

### v2.6.3

**修复**（全面代码审计第三批：缓存/存储加固）

- **修复·缓存文件损坏后翻译全挂**：缓存文件为合法 JSON 但内容非字典（如数组/字符串）时，翻译缓存每条读写都报错、翻译全部失败且持续写坏文件。现按损坏处理自动重建
- **改进·翻译缓存改为 LRU 淘汰**：原先按写入顺序淘汰（800 条上限），近期仍在用的旧译文先被清掉；现命中即刷新，淘汰永远发生在最久未用的条目
- **修复·存储位置迁移可能造成数据分裂**：目标目录已占用时，拒绝发生在移动中途且已搬走的文件不回退——模型/缓存两处各一半、程序却都找不到。现迁移前整体预检，拒绝时旧数据原封不动
- **修复·预设 HF_HOME 环境变量时模型反复重下**：模型缓存判定读环境变量路径、下载与加载写程序默认路径，两侧分裂可致 1.6GB 模型反复下载。现下载/加载/判定三处统一走同一缓存根（自定义存储根用户不受影响）

### v2.6.2

**修复**（全面代码审计第二批：停止/重启时序重构）

- **修复·说完立刻停丢最后一句**（此前 v2.2.1 的修复实际无效）：停止链路存在三层丢弃——先断信号使采集线程的 flush 尾段无人接收、识别/翻译线程 stop 时清空队列丢弃已入队内容、主窗口 running 守卫再拦一层。重构为**排水式停止**：线程改为"排空才退"，识别尾段同步直塞翻译链路，翻译线程停止后保留 15 秒宽限期等尾句进来——最后一句字幕在停止后数秒内补齐上屏
- **修复·切换输入来源时新旧线程并存**：「等旧线程退出」的防御代码因引用已被置空从未生效，切源时新旧采集线程并存抢音频设备、新旧模型并发加载双份内存。修：停止前快照线程引用再等待
- **修复·旧会话迟到信号污染新会话**：停止后快速开始时，旧线程已入队的事件（状态/错误/字幕）会落入新会话——最严重时旧会话的"音频错误"会误停新会话。修：全部回调槽增加会话身份守卫（sender 校验），旧线程信号在新会话中一律拦截
- **修复·翻译队列满丢句后占位卡永久悬挂**：队列满时静默挤掉旧句，对应字幕卡永远停在 "⟳ …"。修：被挤掉的句子立即把占位卡置为"翻译队列繁忙，本句已跳过"终态并清理簿记
- 回归：单元 46→51、集成 77→81 全绿；新增排水停止契约、宽限期消费迟到尾句、会话身份拦截、丢句终态化、切源快照等九项回归锁

### v2.6.1

**修复**（全面代码审计第一批：崩溃面清零）

- **修复·运行中「清空」后迟到译文崩溃**（P0）：清空只删了卡片，占位配对（`_pending`）、攒句缓冲与簿记（`_tgroup`/`_tgroup_by_src`/`_submit_ts`）全部残留——数秒内译文到达即访问已删 C++ 对象，PySide6≥6.6 直接终止进程。修：清空时与停止管线同步清理全部在途状态，迟到译文安全丢弃
- **修复·启动预热中退出崩溃**（P0）：预热线程游离在退出清理机制之外（预热默认开启，启动后立刻退出是高频路径）——主窗口析构销毁运行中线程 → qFatal。修：预热线程支持协作停止（加载开始前立即短路），退出收尾统一接管；已进入模型构造的线程移交孤儿容器自然收尾
- **修复·备援译文 HTML 实体未还原**（v2.6.0 引入）：主引擎故障切备援时，备援译文只还原了缓存副本，实况上屏仍是 `&quot;` 类转义串——同一句话"实况"与"缓存命中"显示不一致。修：备援结果赋值处统一还原，上屏与缓存共用同一值
- 回归：单元 44→46、集成 75→77 全绿；新增备援实体还原 emit/缓存一致性、预热停止短路、清空在途状态清理、退出释放预热线程引用四项回归锁

### v2.6.0

**译文质量专项**（代码审计驱动，六项落地）

- **修复·译文 HTML 实体原样上屏**：Google 免费接口偶发的 `&quot;`、`&amp;` 等转义串此前原样显示并写入持久缓存。修：引擎译文入缓存前统一还原（单层还原幂等，双重转义保留一层）
- **新增·词典整词匹配**（设置-翻译-译文质量）：开启后英文词条仅在整词出现时替换——「strikes=罢工」不再误伤 "U.S. strikes back" 类专名短语，多义词纠错更安全；中文词条始终按原文替换。默认开启
- **修复·词典链式误替换**：两本修正词典此前逐条 `str.replace`——长词条会被先应用的短词条拆坏（live 拆坏 LiveSubtitle）、前一条替换产物可能被后一条二次命中。修：统一走单轮替换器（`app/fixmap.py`），长键优先、一次遍历，替换产物不再参与当轮匹配。误听修正词典与译文修正词典同步受益
- **新增·离线翻译质量档**（设置-翻译-翻译方向）：**高质量**（beam=5，译文更连贯、直译痕迹更少，单句离线耗时约 +0.1s，上屏几乎无感，默认）/ **快速**（beam=2，旧行为）。在线翻译不受影响；切换后下一次离线翻译即生效
- **新增·词典即时生效**：保存「误听修正/译文修正/整词匹配/离线质量」后直接热更新运行中的识别与翻译线程，字幕纠错不再需要重启识别管线（管线重启意味着模型冷加载近 1 分钟）
- **修复·缓存命中率稀释**：翻译缓存键此前对原文零规范化，首尾空格差异即重复翻译；统一首尾去空白后同一句话不再重复请求引擎。存量缓存自然淘汰，无需迁移
- **清理·死配置** `translate_zh_from_zh`：无任何读取点的失效开关，从设置与配置白名单移除（旧配置文件中的残留键静默忽略）
- 回归：单元 38→44、集成 69→75 全绿；新增整词匹配/长键优先/单轮语义/实体还原幂等/缓存键规范化/beam 透传等回归锁

### v2.5.4

**速度与准确度**（用户反馈"翻译/识别速度慢、准确度差、流式上屏感知不到"——先实测定位再动手）

- **实测定位**（turbo+CUDA+Argos，SAPI 标准段）：低延迟模式下识别段中位延迟 **0.49s**、离线翻译 **0.01s**——转写与翻译本身不是瓶颈；**感知"慢"的主因是分段上限**：普通模式连续说话要攒到 14 秒才切句，字幕自然"迟迟不出"
- **低延迟模式默认开启**（新装用户；存量配置不变）：看视频字幕对延迟敏感，6 秒分段上限+收紧判停把端到端感知从"十几秒"压到"1~2 秒"；普通模式仍可手动切回（整理场景句子更完整）。设置页描述同步强化
- **新增·识别精度三档**（设置-语音识别-识别精度）：**快速**（beam=1，实时字幕推荐）/ **均衡**（beam=2）/ **高精度**（beam=5+结合前文语境，长段与 CPU 上明显更慢，适合事后整理字幕）——速度与准确度的取舍显式化，此前 beam=1 是硬编码的隐性取舍。GPU 实测：短段三档延迟几乎无差（0.49 vs 0.51s），长段/CPU 差距显著；**实时看视频请保持快速**，单点识别错误优先用「误听修正词典」定点纠正
- **流式占位更醒目**：原文先出的占位行文案 "⟳ …" → "**⟳ 识别中…**"。说明：占位停留时长 = 翻译耗时（离线 0.01s/在线 0.5~2s），翻译快时一闪而过属正常——"听到哪看到哪"的延迟大头在识别分段，本轮已压
- 新增单元回归锁（精度档参数映射+未知档回退），识别精度档随管线重启生效（预热池同步该参数）

### v2.5.3

**修复**（用户实拍实测反馈三项）

- **修复·字号菜单四档全部打勾**：菜单非互斥且只在构造时设置一次勾选，换档/滚轮调节后旧勾永不消失（用户实测小/中/大/特大四档全勾）。修：勾选态随当前字号同步互斥（±3px 容差归档），任何调节路径（菜单/滚轮/设置页）即时刷新
- **新增·面板支持上下拉长**（用户裁决，恢复 v2.4.0 移除的手动高度）：**拖底缘拉高/压矮**，拖后锁定手动高度（新句在固定高度内滚动不再自动收放）；⋯ 菜单新增"恢复自动高度"、**双击底缘**同样恢复；高度持久化（`overlay_h` 回归，0=自动）
- **修复·双"置顶"概念冲突**（用户指出）：设置页开关"启用字幕面板（置顶）"与面板 📌 按钮的置顶语义打架。修：设置页开关更名"启用字幕面板"（描述说明是否压过其他窗口用面板 📌 控制）；置顶语义专属 📌
- 新增集成回归锁 3 项（66→69）：字号勾选互斥、底缘拉高+手动高度契约、开关无双置顶字样

### v2.5.2

**修复**（随机探索测试轮）

- **修复·面板 tooltip 拦截点击**：悬停状态行/面板 1 秒后弹出的原生 tooltip 窗口会**拦截光标区域后续所有鼠标点击**——随机测试实锤：悬停状态行后再点"清空/⋯/📌/收起/✕"全部"点了没反应"（用户感知=按钮坏了；tooltip 覆盖区域的事件被 tooltip 窗口吃掉，Qt 不转发）。修：移除状态行与面板级的 tooltip（全文本保留在主窗状态行；手势说明由首次手势引导承担）。按钮级 tooltip 弹出位置在按钮外，保留
- 另：自动化注入下工具条右段按钮点击无法定位根因（PostMessage 直达可复现功能正常；点击失灵与高 DPI 缩放三重坐标换算+焦点路由纠缠），已记录待物理鼠标人工复核，不盲目修改

### v2.5.1

**修复**（真实英语新闻 + 离线翻译实测轮抓到的两个"设置不兑现"）

实测条件：CBS News 24/7 真实直播 + large-v3-turbo（CUDA）+ Argos 离线翻译 + 低延迟模式，全流程跑通；离线翻译质量实测良好（"以色列指责伊朗打破了它，并发誓要反击"、人名音译正确），已知直译瑕疵维持备案。

- **修复·启动不恢复字幕面板显隐**：配置里面板是开着的，重启应用后却要等"开始翻译"才出现——"我明明打开了"的设置不兑现。修：启动时按 `overlay_enabled` 直接恢复显示
- **修复·面板滚轮调节在真实使用中永远失效**：Qt.Tool 窗口隐含 `WindowDoesNotAcceptFocus`——面板点击也不激活（实测点击后前台仍是视频/浏览器），系统把滚轮全部发给焦点窗口，Ctrl+滚轮调字号/透明度形同虚设。修：清除该隐含标志。修后实测：**鼠标悬停面板即可滚轮调节，且不抢视频焦点**（前台保持浏览器），正是字幕工具该有的行为
- 集成回归锁随 v2.5.0 已覆盖（66 项），本轮行为改动经实机复验：启动面板自显、悬停滚轮字号 26→28 落盘

### v2.5.0

**悬浮字幕面板深度迭代**（对标豆包 AI 字幕：深度实测截图矩阵 → 结论 → 优化；抓出并修复 3 个隐藏 bug，新增 5 项体验）

- **修复·字号调节从 v2.4.0 起完全失效**：面板化时正文（原文/译文）从未挂 font-size 规则，设置页与工具条 Aa 调字号纹丝不动（截图矩阵 16/30/40 三档像素级相同才暴露）。现在真实生效，影院级 40px 大字幕可用
- **修复·只译文模式面板高度塌陷**：5 行只显示 2.5 行——高度收敛判定比较了被最小值钳住的滚动区高度而非内容需求高度，收敛链提前断
- **修复·窄面板状态行压按钮**：360px 宽时"翻译失败…"黄色长文案与工具条按钮重叠绘制。状态行限宽 = 面板宽 − 按钮区，全文保留在 tooltip
- **新增·双行层次（豆包式）**：原文 = 主字号 72%、更淡的灰；译文 = 主字号加粗白——扫读时主次一目了然，同屏可容纳更多句
- **新增·行卡片化 + 最新句高亮**：每句一个圆角底色块，句界清晰；**最新一句左侧主题色竖条 + 蓝调底色**，新句一眼可辨
- **新增·精简条模式（收起即小条）**：收起不再是"只剩工具条什么都看不到"——正文区换成**最新一句速览**（原文小灰 + 译文大白），新句实时跟随；**单击精简条展开完整历史**，按住拖动仍是移动面板
- **新增·工具条滚轮快捷调节**：**Ctrl+滚轮 = 字号 ±1**、**Ctrl+Shift+滚轮 = 透明度 ∓5**（30–100 钳制），即滚即见效不用进设置页；正文区滚轮仍是历史滚动
- **新增·拖动磁吸贴边**：拖动松手时面板边缘距屏幕边缘 24px 内自动吸附对齐，不再"永远差一点"
- **新增·⋯ 菜单透明度常用档**：60/75/85/92/100% 一键切换（细调仍走设置页滑条）
- 新增集成回归锁 7 项（59→66），覆盖字号生效、高度收敛、状态限宽、精简条、滚轮、磁吸、菜单档

### v2.4.4

**修复**（全面 bug 摸底轮：真实使用每个功能、隔离配置实机走查，2 高危 + 5 中危 + 2 低危全修；详见工作区 BUG-ROUNDUP-20260912.md）

- **高危·首启向导永不弹出**：v2.4.1"排期即消耗"把置位放在排期时，回调守卫查同一标志自我拦截——全新配置启动后三步向导从不出现且无任何报错（老用户 `wizard_done=true` 掩盖了这条路径）。修：排期标志与"已显示"标志分离，回调只在真正弹窗时置位；排期实例不可见时有限顺延（封顶 5 拍）兼顾多实例不挂死。回归锁：排期后不得置已显标志、可见实例弹一次、不可见顺延
- **高危·GPU 静默回落 CPU（三重误导）**：判定"缓存完整"却加载失败（huggingface_hub 1.30 离线完整性校验拒载体积完好的快照，与体积判定脱节）→ 直接降到 CPU 且全程无提示，界面速览卡还按配置显示"（GPU）"，实测识别延迟 30.9s（GPU 基准 4.5s）。修：① 失败时同设备联网重试一次（校验/补全快照，成功即留在 GPU）；② 仍回落时走状态行明示"GPU 加载失败已回落 CPU + 原因"；③ 速览卡改读 AsrThread 实际加载设备，模型就绪即刷新。实机验证：损坏缓存现场下 `model_repaired_online → device=cuda`，GPU 恢复
- **中危·低电平告警不撤回**：一边出字幕一边挂"输入信号过弱：字幕可能无法识别"（主窗横幅 + 面板"信号弱"）。修：字幕成功上屏即撤告警；会话内已证明可识别后，静音期不再重挂（无产出场景另有 25s 无字幕指引兜底）
- **中危·"重新运行首次向导"静默丢弃未保存修改**：向导关闭后无条件回填控件，暂存修改丢失且状态栏误导性显示"所有改动已保存"。修：有暂存时先问"保存并应用/放弃/取消"
- **中危·字幕卡纠错菜单实机不可达**：原文/译文标签的可选中属性让右键弹 QLabel 英文菜单（Copy/Select All），P14 中文纠错菜单永远出不来（集成测试直接调内部构建，从未覆盖真实右键路由）。修：标签右键交还父级卡片菜单（选中 Ctrl+C 仍可用）。实机验证：右键即弹"复制原文/译文/纠正识别/纠正译文"
- **中危·"导出 SRT…"默认给你 txt**：与通用导出共用对话框，默认名与过滤器都是 txt，照文案操作得不到 SRT。修：SRT 入口默认名 `.srt` + 过滤器 SRT 首选（格式仍按扩展名兼容）。实机验证：默认 `LiveSubtitle_*.srt`、导出内容带编号时间轴
- **中危·面板手势引导"只弹一次"失守**：清空后每次都重现三行小抄。修：首条字幕到达即复位，此后空状态回退单行占位
- **低危·导出完成提示路径为正斜杠**（资源管理器打不开）：规范化为 Windows 路径
- **低危·停止翻译后字幕列表凭空消失**：一律切回速览卡，会话记录不可回看。修：仍有字幕卡时停在列表页
- **文案统一**：向导完成页教用户找不存在的「启用悬浮字幕条」勾选项（v2.4.0 已更名"启用字幕面板（置顶）"）；速览卡/托盘/设置页的"悬浮字幕条"旧名一并统一为"字幕面板"
- 新增集成回归锁 8 项（51→59），覆盖上述全部行为契约

### v2.4.3

**新增**（最后一轮把玩建议 A~E 全量落地：面板工具条补齐高频操作）+ **修复**（模拟用户实机走查抓到的高度滞后）

- **A 清空按钮**：工具条新增"清空"，一键清空面板历史（行/占位/最近一句全清）；⋯/右键菜单同源新增"清空面板字幕"，无内容时置灰。只清面板，不动主窗历史与 SRT 导出
- **B 空状态占位**：面板空闲时正文显示暗色占位"字幕将在这里逐句显示"，来字即隐、清空后回归；v2.4.2"无灰块"像素回归锁继续通过（占位文案亮度压在 rgba(255,255,255,72)，混底后仍低于回归锁阈值）
- **C 置顶图钉上工具条**：📌 可勾选按钮，与 ⋯ 菜单"置顶显示"同源双向同步，点击即落盘 `overlay_pin`；勾选态有高亮底色（此前勾选态看不见，模拟用户走查抓到后补 QSS）
- **D 首次手势引导**：面板首次显示时空状态文案升级为操作小抄（拖工具条移动 · 拖右缘改宽度 · 双击贴顶底 · 右键/⋯更多）；挂在 showEvent 上每进程只发一次，主窗按新配置键 `overlay_hint_shown` 落盘、每份配置只弹一次
- **E "↓最新"未读计数**：上滚暂停跟随后每完成一句计数 +1，按钮变红显示"↓ 最新 N"，回到底部（点按钮或手动滚到底）归零复原；占位行不计，只数完成句
- **修复：面板高度滞后一拍甚至停在空闲高度**（v2.4.0 起潜藏，模拟用户实机截图走查抓到）：插入新行的当拍 QLabel 的 sizeHint 还没定型（实机插桩：读到 8px，事件循环后才是 139px），`_relayout` 用过期值定高——三行字幕进来面板纹丝不动，要等下一次内容变化才"补涨"。修：内容变化后排零时器延迟复排，高度未收敛再排一拍（排期即消耗 + 封顶 8 拍防振荡）；实机验证 3 行后面板 100→201px 与内容贴合。新增回归锁"高度贴内容真实成立"
- **视觉诚实化两处**（同一轮走查抓到）：禁用按钮此前因 QSS 强制色看不出置灰（看着能点点不动），补 `QToolButton:disabled` 暗色文案
- 新增集成回归锁 6 项（45→51）：清空+占位（A/B）、图钉同源（C）、首次引导一次性 + 主窗落盘（D）、未读计数（E）、高度收敛

### v2.4.2

**修复**（把玩复验时抓到的视觉缺陷：空闲面板一块灰）

- **面板无字幕时正文是一大块浅灰矩形**：`QScrollArea` 设了透明背景，但它的 **viewport 是独立子控件**，父级 QSS（连 `> QWidget` 选择器）都命中不到它，仍是系统默认浅色底——有字幕时被行遮住看不出来，一空闲就露馅，在深色面板上像坏了一块。修：直接给 viewport 与正文容器挂 `WA_TranslucentBackground`。实机像素采样正文中部 = RGB(27,29,37)（面板本色），灰块消失；并加像素回归锁（空闲时正文中心像素必须为深色）防倒退

### v2.4.1

**修复**（用户上手把玩：面板的两条硬伤 + 一条潜伏挂死）

- **✕ 关闭按钮"没反应"**：v2.4.0 重构时把 `hide()` 误放进 `else` 分支，而主窗永远传了关闭回调，导致该分支永不执行——点✕只把配置 `overlay_enabled` 改成 False、面板却纹丝不动。补回"先隐藏、再回调落盘"（实机验证：点击后面板隐藏=True）
- **右缘调宽"够不着也看不见"**：面板从没开 `setMouseTracking`，不按住鼠标就收不到移动事件，右缘的 `↔` 调整光标**永不显示**；加上命中带只有 10px、贴着圆角边缘，普通人根本找不到。现在：① 开启鼠标追踪；② 命中带加宽到 14px；③ 右缘中央画一列**可见的 `⋮` 拖拽把手**（拖动时变亮），让"这里能拉宽"从隐形手势变成看得见；④ 悬停把手显示操作提示 tooltip（拖动改位置 / 拉右缘改宽度 / 双击贴顶底 / 右键更多）。实机验证：右缘拖拽 514→684 变宽=True
- **顺带根治一条会挂死事件泵的潜伏 bug**：首启向导 `singleShot(400)` 在"多窗口实例 + 嵌套事件泵"下反复弹模态 `exec` 卡死（新增的鼠标追踪测试把它照了出来）。改为**排期即消耗**——进程内只排一次向导，彻底堵住递归/悬空定时器两条路。集成测试连跑验证不再挂死
- 新增回归锁：✕ 必须真正隐藏、右缘命中带=RESIZE_EDGE、必须开鼠标追踪（防这三点再倒退）

### v2.4.0

**重大改版**（P31：字幕悬浮窗退役，字幕面板上位——用户裁决"旧条删掉，换新样式"）

- **新形态**：深灰圆角不透明面板。顶部**工具条**=🌐目标语言▾ / 原文开关 / Aa字号▾ / 状态 / ↓最新 / ⋯ / 收起 / ✕；正文=**原文(灰)+译文(白加粗)成对左对齐**的历史滚动区，自动跟随最新，**上滚即暂停跟随**并浮出"↓最新"按钮
- **交互诚实化**（旧字幕条"哪里能拖"之谜的根治）：面板是一块实心板——**整板任意处可拖**，工具条是显式把手，右缘拖宽，**双击工具条=顶/底贴边循环**。旧形态的点击穿透、文字紧凑带、四边把手带、描边体系全部随之下岗（那是在给"撒谎的透明区"打补丁）
- **实用功能进工具条**：目标语言/字号/原文开关/收起一键可达；⋯ 与右键同源——打开设置、切换输入源、**复制最近一句 / 纠正识别 / 纠正译文（P14/P29 挪到看得见的地方）**、**导出 SRT**、置顶开关（新）、贴边四向、隐藏
- **高度贴内容**：正文多高板子多高，永不存在"空区吞点击"；宽度主权归用户（初值/拖拽/持久化），**不再随内容横向跳变**（实机插桩抓到 adjustSize 顶宽 450→580 并修复）
- **退役清单**（配置键自动清理）：`overlay_stream`、`overlay_list_mode/list_max`（面板天生历史滚动）、`overlay_h`（高度自动）、`overlay_click_through`（无空区可穿）、`overlay_outline*`（不透明板不需要描边）。保留：总开关/位置/宽度/字号/颜色/透明度/`instant_caption`/`show_source`/显隐热键；新增：`overlay_pin`（置顶）、`overlay_collapsed`（收起态）
- **设置页同步瘦身**：字幕页只留"启用面板 / 同时显示原文 / 流式上屏 + 外观四件套"，紧凑列表模式分区删除；文案改称"字幕面板"
- 实机验收（520px 宽面板）：出字宽度锁定 True、工具条无挤压、右键菜单全项可用、O 热键往返正常、P26 遥测继续打点

### v2.3.21

**修复与新增**（体验报告 P28/P29：悬浮条把玩报告的两条遗留账）

- **连续流模式的空白处点击穿透**（P28）：v2.3.19 的穿透只覆盖单条/跑马灯模式——而日常用户开的是**连续流**，整窗依旧吞鼠标。翻案：旧版"整窗可交互"保护的是一个不存在的手势（只读文本浏览器关着滚动条，用户根本滚不动），代价却是继续挡视频进度条。现在连续流的交互区同样按**已渲染文字带**压缩：文字不满窗时上方空区穿透，文字带/四边把手/状态行仍可点；列表模式（真有滚动条）维持整窗保护。实机复验（流式开+单句上屏）：中部空区 `WS_EX_TRANSPARENT=True`、文字带与把手 `False`，探针 PASS
- **悬浮条右键菜单新增"纠正最近识别…/纠正最近译文…"**（P29）：P14 把一键纠错做进了主窗卡片，但看字幕的人整天盯悬浮条不开主窗——最该长把手的地方没把手。现在悬浮条右键即弹与卡片同源的预填对话框；无内容时两项置灰。穿透开启后注意：菜单要在**文字/状态行/边缘**上右键（空区已让位给下层窗口）

### v2.3.20

**新增**（P26：延迟自测——把"测速"从搭仪器变成看日志）

- **内置延迟遥测**：每次翻译会话停止时，日志自动写入一行 `pipeline.latency`——识别段（音频切分完成→原文上屏，含判停/排队/转写）与翻译段（提交→译文落地，纯引擎耗时）各自的 p50/p95/max 与样本数。第十三轮为测两个延迟数字折腾了三轮（热键双切、进程环境冲突、标记句被直播声淹没）——从此看一行日志即可。实机首验：`n_reco=4 reco_p50=0.77 | n_tr=4 tr_p50=0.63 tr_max=0.72`
- 管线内部信号契约同步升级：`segment_ready` 随音频段携带切分时刻、`text_ready` 追加第 4 参 `t_flush`（旧格式裸数组全兼容，smoke/deep_windows 不受影响）

**同时封版一项决策**：P27（巨型合并块"分卡显示"）经论证**不做**——按句回分要求源/译文句界对齐（引擎分句随意），错位会把字幕翻乱，风险大于大块卡；维持备案。

### v2.3.19

**新增与改进**（把玩报告三件套 P25）

- **悬浮条空白处点击穿透**（P25a）：单条/跑马灯模式下，文字只占中间一条，但整块半透明矩形一直是"置顶+吞鼠标"的硬板——压在视频上时底下的进度条/暂停键都点不到（把玩报告头号痛点）。现在光标不在**文字紧凑带/边缘把手/状态行关闭键**上时，悬浮条自动设为 OS 级点击穿透（实测取证：Qt 的 WA_TransparentForMouseEvents 在 Windows 上根本不设 WS_EX_TRANSPARENT、点了照旧被吞——真穿透必须直改 HWND 扩展样式），光标探回文字即恢复拖动/右键。设置页与右键菜单均有开关（默认开），连续流/列表模式整窗保持可交互不受影响
- **缩放从隐藏功能变可见**（P25b）：边缘命中带 10px→16px；光标扫到可缩放的边/角时**该边缘亮起一条蓝色描边**——以前只有肉眼难察觉的光标形状变化，没人知道悬浮条能缩放
- **贴边菜单补齐左/右缘**（P25c）："贴到屏幕左侧/右侧"两项，四缘磁吸完整

### v2.3.18

**修复**（第十二轮"随便调"矩阵抓出的速度真雷）

- **攒句头号句等待不再被续片"续命"**（P23）：硬兜底 deadline 此前**每片到达都重置**（now+7s），密集插话里一条小写延续链能把头号句扣 13.4~13.6 秒（实测），5 片链理论上限 ~30 秒。现在取"**逐片 7 秒间距**"与"**组寿命 10 秒绝对上限**"的较小值：单跳真延续（whisper 分片 6~7.5 秒到达）照旧完整合并，多跳长链 10 秒剪断。复验：同一直播段最坏 10.4~10.9 秒封顶（=上限 10s+巡查 1s+译文落地，数学吻合），合并质量不受损
- 同时修正上一版结论：v2.3.17 CHANGELOG 里"8 秒是几何上限"的推导漏看了 deadline 重置逻辑——真上限是"每片续命"，本条即为勘误+修复

### v2.3.17

**改进**（第十一轮真人语流数据揪出的"界面装死"）

- **占位卡始终显示原文**（P22）：关闭"显示原文"（默认）时，攒句/翻译等待期（最长约 7 秒）字幕列表里只剩一排"⟳ …"转圈——系统明明在正常工作，界面看起来像卡死（真人直播实测出现过三条同时占位）。现在等待期用**弱化灰斜体**显示已听到的原文，译文一落地即恢复你的显示偏好。想"只看中文"依旧成立：最终列表仍然只有中文，只是"等待的样子"不再是天书

**同时封版一项分析**（不改代码，留证据）：密集多人插话场景最坏约 8 秒的整句延迟是**几何上限**——whisper 分片到达间隔 ≈6~7.5 秒（6s 音频块+转写耗时），7 秒硬兜底已贴着刀刃；任何"完整句提前送"的捷径都会把 `"…through the night." + "and they expect…"` 这类真延续重新劈成两半。快与准的边界到此为止，属产品取舍而非缺陷。

### v2.3.16

**工程**（P19 的制度化收尾，无功能变化）

- **跨模块信号契约审计**（P21）：把仓库所有带数值的信号（`level_changed` / `text_ready` duration / `progress_pct` / `low_input` / `muted` 等）逐一做"生产量纲 vs 消费阈值"对账——除 v2.3.15 已修的电平错配外**未发现新的活体错配**（progress_pct 全链 0~100 整数一致、duration 秒数消费一致）。审计成果落防：单位契约钉进信号声明处（capture.py / engine.py）、消费槽 docstring（_on_level）与常量注释（QUIET_WARN_S 秒 / QUIET_LEVEL 原始峰值 0~1），配合既有 `t_level_signal_scale` 接线回归锁——下一个"猜单位"的 value>=3 在写下的那一刻就该看见契约

### v2.3.15

**修复**（第十轮速度/形态专项，实测揪出潜伏三版的老 bug）

- **电平守卫此前从未真正生效**（P19）：采集线程发的电平是 **0~1 的比例**（`min(1, level×8)`），而主窗口守卫条件写成 `value >= 3`——这个阈值**永远为假**。连锁后果：①"最近有声"时间戳从不更新，P16 尾句"静默 3.5 秒立送"退化成"任何时候都算静默"，省略号结尾的未完半句被秒送、攒句分片被腰斩；②P8"无声音指引"的电平守卫同理形同虚设；③音量条 `setValue(小数)` 恒为 0——界面反复提示你"看音量条有无波动"，其实那根条从没跳过。现已按 0~1 比例消费（有声阈值 3%、音量条换算成百分比），三重修复一次到位
- **实测澄清**：修复后尾句延迟稳定在 3~4 秒（静默巡查 3.5s + 译文 0.5~1s），第 2 测试句的"whisper 分两片交付"被正确合并为一条译文（此前会被腰斩）；句中翻译延迟约 1.5~2.3 秒、识别延迟约 1~2.5 秒
- **悬浮字幕上屏形态**（用户问题直答）：连续翻译（流式）模式下，每条字幕是**整句一次出现**（抓屏亮度阶梯式跳变，一行一个台阶），**不是逐字打字机效果**——因为流式区每次追加即整体重建富文本

### v2.3.14

**改进与修复**（第九轮"翻译速度"专项）

- **尾句静默立送**（P16）：低延迟攒句的兜底从"单发等满 7 秒"改为 1 秒巡查——**音频静默 ≥3.5 秒**即送尾句（实测尾句从说完 +7.8 秒降到 +1.7~4 秒），实况新闻流节奏明显跟手
- **静默立送加"收尾守卫"**（P16 初版回归修复，第九轮实况抓到）：光有静默不够——whisper 把一句话切成多个分片、分片之间天然有 2~4 秒交付间隙，P16 初版会误判"说完了"当场腰斩整句（"stockpiles of uranium..." / "were transferred away..." / "before the US strikes." 三条各走）。现在**静默 + 末片"看起来完整"双证据**才提前送；省略号/逗号/无标点收尾的未完片继续等延续片（由 7 秒硬兜底保证最终必出）。实况复验：那句铀报道已合并回一条完整字幕
- **缩写不再被腰斩**（P17）：二次分句的切分点从"句号后（哪怕零空格）"收紧为"拉丁句末标点**后有空格且点前不是大写字母**"——`U.S. strikes` 不会再被劈成 `U.` / `S. strikes`；中文句末标点（。！？）后无空格仍直切。第九轮实况 `"effectiveness of U." / "S. strikes on Iran"` 实锤驱动此修

### v2.3.13

**新增**（第八轮实测 P14：词典"到不了"的工具化补救）

- **字幕卡右键一键纠错**：每张字幕卡右键弹出菜单——"复制原文 / 复制译文 / 纠正识别（加入误听词典）… / 纠正译文（加入译文修正词典）…"。纠错弹窗**自动预填**卡片文本（你只需删改成要纠正的词、写上正解），保存即写入配置并原子落盘，下次开始翻译生效。背景：误听/译文词典功能早已就位，但"看到鬼译文 → 打开设置 → 找到词典 → 手打两边文本"链条太长，实测八轮词典一直空着——功能可达性从卡面直接解决

### v2.3.12

**修复**（第七轮实测遗留观察 → 实锤为真缺陷）

- **视频彻底静音时不再"静默失联"**（P13）：WASAPI 环回在输出**完全**无声时根本不产出数据包，而旧代码把"没包可读"当作"跳过本轮一切统计"——结果 Chrome 暂停媒体/整晚静音时，"输入信号过弱"告警永远不触发、音量条冻在最后一个值、字幕无声消失，用户完全无从判断"应用挂了还是视频没声"（第七轮实测 90 秒失联复现）。现在"无包"等价于"无声"：计入静默时长、超半秒主动把音量条归零、达阈值照常弹告警。实机验证：按 J 后全程静默 → 13.2 秒即报 `low_input`（旧代码此时一声不吭）

### v2.3.11

**改进**（第七轮实测：固定噪音清除）

- **"无识别结果"指引窗口 15 秒 → 25 秒**：六轮实测发现该指引在"打开网页随即按开始"的主场景**每次必误报**（YouTube 从打开到出声普遍要 5~15 秒，15 秒窗口必被击穿）。25 秒仍能兜住真静音/选错设备场景；P8 电平守卫（有声音活动顺延复检）逻辑不变。日志键同步更名 `pipeline.no_segments_hint`（带 window_s 字段）

### v2.3.10

**修复**（第六轮报告 P11）

- **模型"已下载"判定跟随 huggingface_hub 实际解析路径**：用户（或工具）预设 `HF_HOME` 环境变量时，模型实际缓存在环境变量指向的目录，而预热/加载的"已下载"判定此前只看应用自己的数据目录——两侧不同步会让启动预热误判"未下载"直接跳过（隔离环境实机实锤），用户白等冷加载。现在判定与环境变量同源；正常用户两侧同值，行为不变。新增单元测试锁行为

### v2.3.9

**修复**（第六轮实机验收抓到 v2.3.8 的"半成品"）

- **低延迟攒句兜底窗口 2.5 秒 → 7 秒**：v2.3.8 的大小写判据方向正确，但 2.5 秒静默兜底**小于 6 秒分片周期**——连续语音里前半句会在后半句到达前先行送出，攒句实际永不发生（重播长句实锤：两段各自成键）。改为 7 秒后实机验收通过：`"Officials said…night." + "and they expect…"` 合并为一句翻译，译文主语连贯（"…救援行动持续到夜间。他们预计全面调查还需要几周时间。"）。连续语音中该计时器永远到不了期，只在真正停顿/尾句时兜底，多付的等待仅发生在句尾
- 新增回归锁：集成测试断言兜底窗口必须大于分片周期——防止再犯"测试手动模拟计时器、掩盖参数错误"的同类盲区

### v2.3.8

**修复**

- **低延迟攒句的真实有效性**（P9 v1 被实测打穿）：v2.3.7 用"句末标点"判断句子收尾——但 whisper 对 6 秒音频切片**会自行补句号**，长句复验实测每片都带句号、逐片即送，攒句形同虚设。判据改为**首字母大小写**：小写开头=上句延续（并入），大写/CJK/数字开头=新句开始（先送已攒整句）——该信号在真实数据（"and authorities…"式碎片）中验证有效；辅以 5 片上限与 2.5 秒静默兜底
- 清理失效判据的死代码（ends_sentence），单测/集成测试按新语义重写（含"whisper 自补句号也不触发"的反例断言）

**工程**

- 集成套件退出码定性（faulthandler 抓栈实证）：偶发 0xC0000005 发生在主线程进程收尾路径（Qt DLL 卸载竞态），无肇事后台线程、全部测试与报告已完成——按项目既有惯例**判定以 test_report.txt 的 TOTAL 行为准**；TerminateProcess 保留（它已根除"挂死不退出"）

### v2.3.7

**改进**（模拟用户第五轮报告 P9/P10）

- **低延迟两轨制——"上屏碎、翻译整句"**：低延迟模式下碎片照旧即时上屏（快），但翻译自动攒到整句再送（句末标点 / 满 3 片 / 2.5 秒静默三判据）——解决实测抓到的"and authorities…"式小写开头半截句独立翻译、丢主语译文差的问题。整句译文落组内末卡，前碎片卡只留原文延续上文；关闭低延迟时行为逐字节不变
- **日志按天滚动**：`app.log` 只留当天，跨天自动归档为 `app.log.YYYY-MM-DD`——多日混排曾困扰排障

**工程**

- 集成套件退出竞态根治：34/34 全过但退出码偶发 0xC0000005——`os._exit` 仍走 CRT 触发 Qt 原生线程 DLL_PROCESS_DETACH 竞态，改用 `kernel32.TerminateProcess` 直接终结（连跑 3 次 exit=0 实证）；另新增存活线程诊断输出
- 低延迟设置文案同步：不再夸大"译文变差"代价（P9 已修），如实说明"翻译自动攒整句、显示上句子可能切短"
- 单元测试 33→35（攒句判据/日志滚动）、集成测试 33→34（P9 全链路真实配对）

### v2.3.6

**新增**（体验报告 P7）

- **译文修正词典**（设置-翻译，与识别侧"误听修正词典"对偶）：每行一条 `错误译文=正确译文`，保存后对翻译结果精确替换——引擎翻错/翻反的多义词（实测案例：*"pace the rate of AI development"* 被翻成"加快"，语义完全相反）用户可自助一键纠偏；修正在**上屏前**应用，缓存里的存量错译同样被修正显示，词典可随时增删

**修复**（体验报告 P8）

- **"无声音"指引抢跑**：视频刚打开还在缓冲时，15 秒指引会误报"请确认设备正在播放声音"（CBS 实测轮抓到）。现在指引前先看电平：近 15 秒有过音量活动就自动顺延复检，不冤枉用户

**工程**

- **集成测试套件稳定性根治**：本轮暴露出挂点漂移的原生挂死（一次卡贴边测试、一次卡进程退出）——①headless（offscreen）环境预热直接 no-op（桌面 UX 功能不做无头线程churn）②贴边测试改纯几何验证（去掉 offscreen 下偶发死锁的 show+事件泵）③套件新增逐项进度打印、120 秒 faulthandler 看门狗（挂死自动 dump 线程栈）、跑完 `os._exit` 强制退出免疫 Qt 析构竞态。连续 3 次全绿验证
- 单元测试 32→33、集成测试 31→33

### v2.3.5

**新增与改进**（模拟用户 CBS 直播实测轮 P5：按"开始翻译"后 GPU 冷启动近 1 分钟）

- **启动时预热模型**（设置-语音识别，默认开）：软件一启动就在后台把已下载的模型加载进 GPU/内存，点「开始翻译」几乎秒就绪——实测按键到就绪从 **37.5 秒降到 0.013 秒**（日志 `asr.model_reused`）。只预热已下载模型，**绝不自动下载**；想省显存可在设置关闭
- **加载文案如实**：GPU 首次加载明说"约 1 分钟，仅第一次，之后秒开"，不再含糊的"几秒到几十秒"让用户误以为卡死
- **预热可观测**：新增 `asr.prewarm_start / prewarm_done / prewarm_failed` 日志键——本轮实测正是被"预热静默无日志"坑了一次排查，可观测性即修复的一部分
- 集成测试 30→31（预热不触发下载的安全边界）

### v2.3.4

**修复**

- **纯标点碎片漏网**：CBS 新闻实测轮抓到 `".."` 这样的无内容碎片仍进入字幕与翻译缓存——v2.3.1 的内容过滤只作用于 Whisper 原始分段，而长句"二次分句"（14 秒长文按句末标点拆开上屏）产生的标点尾巴绕过了过滤。现切分后的每一片再过一次内容检查（单元测试 32 项）

### v2.3.3

**新增**（模拟用户体验报告 P1/P2 落地）

- **低延迟模式**（设置-语音识别，默认关）：分段上限 14 秒→6 秒、静音判停 0.45→0.30 秒、自适应区间收紧至 0.20~0.40 秒。直播/新闻连读场景字幕上屏滞后从 ~30 秒降至 ~10 秒级；代价是句子可能切短、译文上下文变少，录播/电影建议保持关闭。保存后重新翻译生效
- **悬浮条右键"贴到屏幕顶部/底部"**：一键归位并持久化——以前压住网页播放器控制条只能手动拖拽

**工程**

- 单元测试 30→31（低延迟分段器：6s 必切、默认模式不误切）；集成测试 29→30（贴边行为与位置回调）

### v2.3.2

**改进**（模拟用户体验报告 R6 遗留缺口 G1/G2 清零）

- **悬浮字幕条状态常驻仪表盘**：主窗口空页面"当前配置"卡新增"悬浮字幕条"一行——关闭时琥珀色显示"已关闭 · 按 Ctrl+Alt+O 打开"，开启时显示"已开启（可拖动位置）"。解决"用户自己关了悬浮条，软件从不提醒、自己都忘了"的真实痛点；热键/托盘/设置页任何路径开关都实时同步此行
- **在线引擎不可达事前横幅**：以前 Google 探测失败只会静默回退（日志里有，但用户看不见），整场翻译频繁出错才知道不对劲。现在开始翻译时探测失败立即出橙色横幅，携带具体失败原因（超时/限流/断网），并提示"到「设置-通用」配置代理"；主引擎自动恢复（每 60 秒重探）后横幅自动撤销；显式指定 Google/MyMemory 时同样启动即探测并预警
- 集成测试 26→29：G1 状态行跟随、G2 横幅持续性、探测失败原因收集

### v2.3.1

**修复**（用户实拍三连击）

- **识别模型"检查更新"必失败（401）**：检查路径仍硬编码 `Systran/faster-whisper-*`，而 large-v3-turbo 实际在 mobiuslabsgmbh 仓库（v2.0.9 修下载/加载时漏了这条路径）。现统一走 `model_repo_id()` 解析，并像下载一样做 hf-mirror 镜像回退，主站不通不再干等超时
- **托盘/悬浮条右键菜单悬停无高亮**：全局 `QWidget` 底色吞掉了系统高亮态。补齐 `QMenu::item:selected` 样式（深蓝底白字圆角，与整体风格一致）
- **纯标点垃圾段穿透过滤器**："....." "？？？" 这类新闻转场/呼吸段残留会显示成字幕——幻觉过滤按概率判断拦不住它们（实测漏网），现无条件滤除（不含任何字母/数字/汉字的段直接丢弃）

**改进**

- **重模型 + CPU 常驻预警**：medium / large-v3-turbo 跑在 CPU 时字幕会越拖越晚永远追不上（CBS 新闻实测主要痛点），现在开始翻译即出橙色横幅提示换 small 或启用 GPU，不再静默卡顿
- **引擎探测失败原因落日志**（`translate.probe_failed`）：auto 模式回退到 MyMemory 时不再无声——日志记录 Google 探测失败的具体原因（限流/断网/超时），排障不再两眼一抹黑

**CBS 英语新闻实测结论（同一视频 A/B 对照）**：`large-v3-turbo + 强制GPU + 自动引擎` 75 秒产出 34 段、约 2.2 秒/段实时跟手；`medium + CPU + Argos` 组合严重滞后且直译错译明显。RTX 20/30/40/50 系显卡用户建议在「设置-语音识别」选最大模型 + 强制 GPU。

### v2.3.0

**工程重构（用户可见行为不变）**

- **声明式设置框架全量迁移**：15 个标准设置行（识别语言/计算方式/幻觉抑制/Silero VAD/翻译引擎/目标语言/悬浮条开关/同时显示原文/连续输出/流式上屏/列表模式/列表条数/关闭行为/自动开始/历史条数）收敛到 `_STD_ROWS` 一张声明表——控件构建、信号接线、两处同步点回显全部表驱动。新增一个标准设置从"改 5 处"降为"3 处数据登记"；`load_from_config` 与 `_set_widgets_from` 两份逐行重复的同步清单（历史上"漏改一处"类 bug 的温床）合并为一
- 复合控件（模型下拉+管理按钮、颜色选择、透明度滑条、代理行、热键输入、误听词典、设备选择）保持手写，行为不变
- 顺手修正两处：① 「识别语言」下拉里"自动检测"重复出现两次（旧循环未排除 auto 项）；② 集成测试 `max_history` 配置泄漏污染后续测试的卫生问题——被新增的覆盖测试当场抓获，正是该测试价值的自证
- 新增集成测试「声明式标准行表全覆盖」：逐行校验键登记/控件属性/配置回显/暂存语义（26 项）

### v2.2.14

**新增**

- **下载剩余时间 ETA**：识别模型下载横幅与语言包下载进度实时显示"X.X MB/s，剩余约 X 分钟"（按滑动实测速度估算；起步 3 秒内与临近完成阶段不显示，避免大误差惊吓）。用户实测反馈驱动

**调整**

- 移除「全屏时自动隐藏悬浮条」设置（v2.2.12 新增）：用户真实环境实测后明确"不需要"，功能代码、设置行、接线测试与配置残留键一并清除，保持设置页最小必要

### v2.2.13

**修复**

- **配置速览卡版式别扭**（用户实拍反馈）：热键行"开始/停止 · 显隐悬浮条"过长换行后，第二行被提示行压住重叠。根因是 Qt word-wrap 标签的换行高度不会通知父布局重排，v2.2.9 的 adjustSize 修复又跑在宽度定稿之前、从未真正生效。现改为**热键拆两行显示**（每行短文本永不换行，从结构上消除该类问题），卡片按最长单行自适应收窄；哪一行热键未生效哪一行标红
- 卡片内标签渗出深色"补丁"底色（全局 QWidget 底色与卡片底色色差），现 SidePanel 内标签统一显式透明

**工程**

- 文字裁剪探测盲区补强：word-wrap/多行标签改按"当前宽度换行后的需要高度"比对（此前只比单行高度，正是漏掉本 bug 的原因）。git worktree 挂修复前代码做双向验证：增强探测稳定报出旧 bug（need=36/have=20），修复后代码报 0

### v2.2.12

**新增**

- **首启向导按硬件预选模型**：检测 GPU/显存（ctranslate2 实际可见的 CUDA 设备，不依赖 torch）后自动勾选推荐档并标注"⭐ 按你的硬件推荐"与理由；用户主动改过模型则一律尊重原选择不覆盖。大显存 GPU 用户不再需要在 tiny/small 里自己猜
- **全屏时自动隐藏悬浮条**（设置-显示新开关，默认关）：检测到真全屏窗口（无标题栏且铺满显示器，排除桌面与普通最大化窗口）时暂时收起悬浮条，退出全屏自动恢复；边沿触发设计——全屏期间你手动显示的字幕不会被轮询再次收起，首次隐藏有托盘气泡说明
- **"正在聆听"呼吸反馈**：模型就绪但还没有任何字幕时，状态灯呼吸闪烁 + 状态行明确提示"播放声音或说话即可出字幕"，首段上屏或停止后自动熄灭——开始翻译后前 15 秒不再是"不知道死没死机"的静默期
- **SRT 长句自动折两行**：优先词边界（英文）、其次标点（中文），单行 ≤44 字符，不丢内容；播放器与剪辑软件观感更接近人工字幕

**工程**

- CI 补 `pull_request` 触发（构建/发布步骤本就有 tag 门控，PR 只跑测试闸门）
- 集成测试 21→26 项：折行规则、硬件推荐纯函数、向导预选与用户优先、全屏开关接线、呼吸起停时机

### v2.2.11

**新增**

- **SRT 时间轴字幕导出**：导出对话框新增「SRT 字幕 (*.srt)」格式，带标准编号与时间轴（时长优先取 Whisper 真实语音时长，相邻条目自动夹紧），可直接加载进播放器或剪辑软件；纯文本导出格式保持逐字节不变。新增确定性集成测试覆盖时间轴与夹紧逻辑（集成测试 20→21 项）

**修复**

- **速览卡热键谎报可用**（真实占用场景实测复现）：配置了热键但注册失败（组合被其他程序占用）时，速览卡仍照常显示"Ctrl+Alt+X 开始/停止"——界面承诺了做不到的事。现以**实际注册结果**为准显示：未生效的组合标红并追加"（未生效）"，全局关闭时显示"全局热键已关闭"；注册状态变化即时刷新速览卡
- **无声音指引可能永不触发**：「30 秒零产出指引」原从点开始翻译起算且单发，模型加载超过 30 秒（首次下载/大模型 CPU）时计时器先于就绪到期，指引永不显示。现改为**模型就绪后重新计时 15 秒**（就绪快路径兜底保留），提示文案与日志键同步更名（`pipeline.no_segments_15s`）
- **首启向导热键写死**：向导固定教"按 Ctrl+Alt+O 显隐悬浮条"，用户改过热键组合或关闭全局热键时会教错。现读取实际配置动态生成该提示行

**改进**

- 模型下载横幅新增**慢速探测**：20 秒内不足 5MB 时追加"速度慢？到「设置-通用」配置代理可显著提速"提示
- 离线引擎预期管理：设置页引擎说明与语言包提示补充"Argos 为逐句直译、多义词易翻错（如 trunks→战线），追求通顺用「自动」"；速览卡引擎名同步标注"离线翻译包（直译）"（真实浏览器视频评测实测的直译腔反馈）
- 文案纠偏：悬浮条复选框说明从"屏幕上方"改为准确的"默认屏幕下方居中、可拖任意位置"，并注明**不必进设置页**——托盘菜单或热键随时可切（可发现性）；关闭进托盘的气泡补充"右键可退出/切来源"指引；离线包体积提示"约 70MB"修正为实测"约 80MB"

### v2.2.10

**修复**

- **速览卡算力档显示错误**：读的是不存在的 `compute_type` 键（恒为 None）→ GPU 用户速览卡永远显示"CPU"，与实际配置 `asr_device='cuda'` 矛盾。改为读 `asr_device`，GPU 配置正确显示
- 仓库卫生：`.gitignore` 补全（`build_env/` 大体积构建物料 ~680MB、测试/探测产物、含敏感信息的会话归档），避免误入库

### v2.2.9

**修复**

- **主界面文字显示不全**（用户截图反馈）：新增程序化文字裁剪探测（`scripts/probe_text_clip.py`——真实 Windows 平台逐控件比对"文字所需宽高 vs 实际宽高"，offscreen 无字体环境测量不可靠故不采用），定位并修复速览卡的 5 处裁剪：热键行（长组合需 490px/可用 326px）、提示行（需 416px/可用 392px）、四行键值对的字体高度不足。修复：面板上限宽度 430→520、长文本自动换行、行高按字体下限给足（键 18px/值 20px）、刷新后重算尺寸；复测 **TOTAL CLIPPED: 0**

### v2.2.8

**改进**（全面测试排查 + 用户视角界面审查批次）

- **新增 20 项集成测试套件**（`tests/test_integration.py`）：配置生命周期/翻译缓存防清库/重采样三采样率/分段器/悬浮条三模式互斥/连续流路由/超长淘汰/字幕卡生命周期/裁剪悬挂引用回归/导出格式/热键解析与安全约束/显隐切换/防抖窗口/设置字段表完整性/三套 QSS 括号配平/向导构建/退出清理链——除真实模型推理与音频设备外全部可自动化路径
- **托盘菜单补齐承诺功能**：此前速览卡提示"托盘右键可快速切换输入来源"但菜单里根本没有——现新增「显隐悬浮字幕条」「切换输入来源」「打开设置…」（另修正速览卡提示文案撒谎问题，与 v2.2.6 的 Ctrl+Alt+O 同款错误）
- **设置保存后界面实时刷新**：`settings_saved` 信号此前从未被连接——保存后速览卡一直显示旧模型/旧引擎，现在保存即刷新
- 修正速览卡提示文案：不再罗列具体热键（以实际配置为准），托盘功能按实际菜单描述

### v2.2.7

**修复**

- **【严重】显隐悬浮条热键按一次等于没按**（v2.2.6 引入的真实按键 BUG，用户实测反馈）：Qt Windows 事件分发器会把**同一条 WM_HOTKEY 两次送达原生过滤器**（最小复现实证：单次注册 + 单次 SendInput → 过滤器收到 2 条同 id 同 lParam 消息）。主热键多年来靠 `toggle_running` 的 0.25s 防抖侥幸掩盖；显隐热键回调无防抖 → 隐藏后立刻又被触发显示 = 按了没效果。修复：过滤器分发层按热键 id 做 60ms 去重（双发只放行第一发）+ 显隐回调 250ms 业务防抖双保险；主热键同样受益
- **悬浮条显隐热键注册失败静默**：组合被其他程序占用时毫无提示（按键"没反应"的另一种成因）——现在托盘气泡 + 设置页状态行双重上报
- 验证方式升级：新增真实 SendInput 按键端到端探测（注册→系统消息→过滤器→回调→悬浮条切换全链路），修复前复现、修复后 PASS

### v2.2.6

**新增**

- **「显隐悬浮条」全局热键（默认 Ctrl+Alt+O）**：提示文案承诺过的热键补齐实现——任何界面按键即可显示/隐藏悬浮字幕条（隐藏时同步配置，显示时强制恢复并同步设置页）；可在「设置-通用-悬浮条显隐热键」改键或清空禁用；与开始/停止热键相同时自动忽略避免冲突

**修复**

- 空状态速览卡"翻译引擎"读错配置键显示 None（`translate_engine` → 实际键 `engine`）
- 速览卡热键提示改为显示实际配置值（不再硬编码文案）

### v2.2.5

**改进**（主窗口视觉三连）

- **字幕卡聚焦态**：最新一条字幕卡亮蓝强调边框，历史卡文字渐隐——一眼锁定"正在说的这句"，长会话浏览不再迷失
- **关键提示横幅**：模型下载进度 / 识别积压丢段 / 翻译连续失败等排障信息从状态行分离，改为琥珀色/橙红色独立横幅（带底色描边），不再被截断、不再淹没在常规状态里；恢复正常自动消失
- **空状态仪表盘**：未启动时主窗口显示当前配置速览卡（识别模型+算力档 / 翻译引擎 / 音频来源 / 热键），附热键与托盘提示——空窗口变成可用的快速检查面板

### v2.2.4

**改进**

- **设置页控件视觉统一**：勾选框（✓）重绘为圆角描边样式——选中时品牌蓝填充+白色对勾点位，悬停高亮；数值调节框（QSpinBox）对齐下拉框的深色圆角风格（含悬停/聚焦边框、禁用态）；所有行内紧凑控件统一 26px 高并垂直居中对齐，不再参差

### v2.2.3

**修复**

- **「只显示译文」在连续流里不生效**：设置页/右键切换 show_source 后悬浮条内存状态不同步，原文照旧流入——现 `apply_overlay_from_config` 统一同步；"只显示译文"时原文段直接不入流
- **连续流内容超出悬浮窗高度被裁**（必须手动拉大窗口往下滑）：高度改为内容自适应并钳制上限（3~8 行），用户手动调整过悬浮窗时尊重其高度并保证最少 3 行可见
- **长文档追加卡顿**：常规追加改为光标 `insertHtml` 增量写入（不再每次全量 `setHtml` 重建），只在超长淘汰时整体重建一次

### v2.2.2

**修复**

- **【严重】悬浮条连续输出模式弹出"莫名其妙的 Windows 窗口"且字幕区空白**（v2.2.0 引入）：连续输出的流式控件创建后**遗漏 `layout.addWidget()`**——QTextBrowser 无父控件时成为独立顶层窗口（带标题栏、可拖动，用户看到的就是它），而所有连续输出内容都写进了这个没显示在悬浮条里的孤儿控件。现已正确挂进悬浮条主布局：开启连续模式后，原文/译文就在悬浮条内部连续累积、自动换行滚动
- 端到端验证：stream_view 父子关系、内容在悬浮条几何内、连续追加顺序全部通过

### v2.2.1

**修复**（后端+UI 深度审计批次）

- **【高】零翻译会话退出会清空整个持久翻译缓存**：`TranslationCache` 懒加载未触发时，退出路径的无条件 `save()` 把内存里的空 dict 落盘覆盖 `trans_cache.json`——最多 800 条历史缓存全灭（快速开关管线即可触发）。现未加载时禁止写盘
- **【中】迁移"目标目录非空"的专属报错被通用异常处理吞掉**：用户选了已有数据的目录，看到的却是错误的"空间不足/杀软占用"指引——拒绝类异常单独定义并透传
- **【中】停止采集时"补发最后一句"永不生效**：`not self._stop` 条件与循环退出条件互斥，尾句在其设计的唯一场景被丢弃——去掉该条件
- **【中】加载期停止的孤儿线程与重启线程并发构造模型**：双倍内存/显存占用、池缓存互相覆盖——构造完成后先检查停止标志，不再把已停止会话的实例入池
- **【中】抗混叠 FIR 逐块独立卷积的边缘效应**：每 30ms 块补零卷积产生 33Hz 周期性调制——改为跨块有状态滤波（尾部缓存衔接）
- **悬浮条占位无视"只显示译文"**：识别先上屏阶段原文仍闪现 1~3 秒——占位按 `_show_source` 过滤
- **模型下载弹窗按 Esc 绕过"下载中"守卫**：Esc 走 `reject()` 不经过 `closeEvent`（Qt 官方文档确认）——在 `reject` 层同样拦截，防运行中 QThread 被析构崩溃与双开下载
- **设置"取消/关闭放弃改动"不还原悬浮条显隐预览**：勾过"启用悬浮字幕条"再取消，悬浮条残留显示——放弃时同步还原

### v2.2.0

**改进**

- **连续输出重做：真·连续文本流**（v2.1.6-2.1.8 的"逐句替换"彻底退役）——译文/原文不断**累积追加进同一段文字**，满了自动换行、自动滚到最新，超长自动从头部淘汰最旧内容（保 700 字尾部）。这才是"听到哪显示到哪、完全不间断"的形态
- 连续流按角色着色：原文浅色小字、译文白色主文，同段落自然衔接

**修复**（全面 BUG 排查批次：三路并行审计 + 本地复验）

- **流式占位队列生命周期**：主窗口字幕被 max_history 裁剪时，`_pending` 里的悬挂引用会让迟到的译文调用已销毁的 C++ 对象（潜在崩溃）——裁剪时同步移除配对；`stop_pipeline` 时清空占位队列
- **deep-test 日志上传路径**：`$env:USERPROFILE` 在 upload-artifact 的 with 输入里不展开，排障日志静默丢失——改为先复制到 workspace 相对路径再上传
- setup.iss 头注释残留 1.8.1 旧版本号

### v2.1.8

**新增**

- **悬浮窗可拖边调整大小**：四边/四角均可像浏览器窗口一样拖拽缩放（光标随边缘变化），手动调整后尺寸固定持久化（字幕刷新不再改变窗口大小），右键"恢复自动大小"一键还原
- **右键"只显示译文"**：勾选即只显示译文行（隐藏原文），立即生效并持久化，与设置页"同时显示原文"双向同步

**改进**

- 跑马灯（连续输出）过渡动画：旧句淡出(160ms)→换字→新句淡入(200ms)，快速连句自动合并只保留最新

### v2.1.7

**改进**

- **连续输出模式样式重做（字幕墙）**：替换简陋的终端式文本——每句一个块（原文小字置灰 + 译文大字白色），**越旧的句子越透明**（渐隐效果），**最新一句全亮加粗**；幕上最多保留 6 句，超出自动滚底淘汰；滚动区域鼠标穿透，拖动悬浮条不受影响；字号/颜色随设置联动

### v2.1.6

**新增**

- **悬浮条连续输出模式**：设置 → 显示 → 「悬浮条连续输出模式」——开启后悬浮条不再逐句替换，而是不断**累积追加**：原文浅色行先落，译文白色行随后，满了自动换行、自动滚到最新（滚动终端式字幕，优先级高于列表模式；主窗口字幕列表不受影响）。关闭即恢复逐句替换

### v2.1.5

**新增**

- **流式上屏开关**：设置 → 显示 → 「字幕流式上屏（原文先出）」——开启（默认）即 v2.1.4 的两段式（原文先出、译文原地补齐）；关闭则回到旧行为（识别+翻译都完成后一次性上屏整条字幕）。运行中修改保存后对后续字幕即时生效
- 修复开关读取时 `Config.get` 不支持默认值参数的调用错误

### v2.1.4

**新增**

- **流式两段式字幕（实时感补齐）**：识别文本**立刻上屏**（译文位置显示"⟳ …"占位），译文就绪后**原地补齐**、不再新建卡片——听到哪显示到哪，不等翻译。此前 medium CPU（约 10 秒/段）或网络慢时界面长时间只有"翻译中…"没有任何内容；主窗口与悬浮字幕条（含列表模式）同步支持
- 占位去重：队列里还有待翻句时不重复刷占位；管线重启/卡片被裁剪时译文回退新建卡片（兜底兼容旧行为）

### v2.1.3

**修复**

- **一键安装按 Python 版本智能选择方案**：PyTorch 官方源最高只发布到 Python 3.13，3.14 用户安装必报 `from versions: none`（用户实测）——现 ≥3.14 自动改装 **NVIDIA 独立运行时包**（`nvidia-cublas-cu12` + `nvidia-cudnn-cu12`，约 700MB，纯二进制轮子不挑 Python 版本）；≤3.13 仍装 CUDA 版 PyTorch
- **GPU DLL 加载机制修正**：此前 `add_dll_directory` 注册的目录不在 CTranslate2 标准 `LoadLibrary` 搜索路径里（它按名加载 `cublas64_12.dll` 走 PATH）——现把运行时目录**前置进 PATH** + `ctypes.WinDLL` 直接加载验证；本地实测同一 GPU 验收探针从"0 条挂死"变为"**40 秒 5 条字幕全出**"（medium/float16）
- 安装按钮/完成提示文案统一为"CUDA 推理运行时"

### v2.1.2

**修复**

- **"一键安装 CUDA 版 PyTorch"按钮在最需要它的人面前被藏掉**：旧显示条件是"CUDA 设备数=0"，而"驱动可见显卡、运行时缺失"（设备数=1，正是会挂死的状态）的机器恰好不显示——现按**运行时三态**判定：未装/CPU 版 PyTorch → 显示安装（已装 CPU 版显示"升级为 CUDA 版"），CUDA 版就绪才隐藏
- **检测摘要新增"CUDA 运行时（PyTorch）"一行**：三态显示（已装 CUDA 版/已装 CPU 版/未安装），引导文案据此给准确结论（不再笼统）
- **强制 GPU 前置运行时检查**：选「强制 GPU」时先验证 CUDA 版 PyTorch 可用并**预载 cuDNN/cuBLAS 进程**（此后 CTranslate2 按名解析 DLL 即命中）——未装/CPU 版/驱动异常自动回落 CPU，就绪提示说明原因；从机制上消灭此前的 GPU 推理挂死
- 一键安装加 `--force-reinstall`（已装 CPU 版 torch 时 pip 原地跳过的问题）

### v2.1.1

**新增**

- **计算方式三档明确化**：「CPU 模式（通用）」/「**强制 GPU**（需先装好 CUDA 运行时）」/「自动（安全档 = 优先 CPU 稳定运行）」——强制 GPU 只用显卡加速（不可用时自动回落 CPU 并在就绪提示中说明原因与出路），自动是稳定优先的 CPU 档。GPU 检测引导文案同步更新

### v2.1.0

**改进**

- **HF 下载智能凭据**：模型下载默认携带本机 Hugging Face 凭据（有有效令牌的用户享用更高速率限额、消除"未认证请求"警告）；令牌失效/无效（401/403）时**自动退回匿名下载**——兼顾两端：有令牌者更快，无令牌/坏令牌者照常可用
- 配套 v2.0.9 的 401 根治仍完整保留：本机过期令牌不再阻塞下载

### v2.0.13

**修复**

- **CI 冒烟判定改为结果文件制**：Qt 应用收尾竞态在 CI 环境连 `os._exit` 前的 flush 都可能 abort（v2.0.12 实测 PASS 后仍 exit 1）——冒烟脚本断言通过后先把 `PASS/FAIL` 写入 `smoke_result.txt`，CI 步骤以该文件内容为最终判定，退出码仅作参考。本地同条件复测：结果文件 PASS + exit 0

### v2.0.12

**修复**

- **CI 冒烟测试"PASS 后仍报失败"**：Qt 应用 + `sys.exit` 在解释器收尾阶段存在 QApplication 析构与后台线程销毁的竞态（本地复现：断言全过、进程仍以 abort 退出）——测试脚本断言完成后改为 `os._exit` 硬退出，跳过 Qt 析构阶段。仅影响测试脚本，应用本体不受影响

### v2.0.11

**修复**

- **「自动（优先 GPU）」在有 NVIDIA 卡但缺 CUDA 运行库的机器上静默挂死**（v2.0.10 用户实测暴露的深层根因，也是此前"翻译没反应"的真因之一）：`get_cuda_device_count` 只证明驱动能看见显卡，不代表 cuDNN/cuBLAS 运行时齐备——缺失时 CTranslate2 推理会无限挂死（7 秒音频 86 秒无返回），表现为永远"正在聆听"、队列塞满、零字幕。现「自动」一律走 CPU（必定可用，就绪提示如实标注"auto 未启用 GPU"）；**显式选 cuda** 才走 GPU，加载失败自动回落 CPU 并落日志
- GPU 加速的正确启用路径（源码运行）：「检测 GPU 环境」→ 一键安装 CUDA 版 PyTorch → 计算方式显式选 cuda

### v2.0.10

**改进**

- **GPU 检测按显存给出各模型档位建议表**：检测现在读取显存容量，按 tiny/base、small、medium、large-v3-turbo 四档逐一给出"可 GPU 运行 / 显存不足保持 CPU"的明确结论与预期速度（如"small：可 GPU 运行（GPU 下约 1-2 秒/段，字幕跟手）"）——不再只有笼统的"medium 需约 5GB"
- 计算方式文案直白化：「自动（有 NVIDIA 显卡选这个 = GPU 加速）」+ 说明"自动 = 优先 GPU、不可用自动回落 CPU"，并指引点「检测 GPU 环境」确认生效（就绪后状态栏显示"GPU · CUDA 加速已生效"）
- GPU 检测摘要区新增"显存"一行
- 顺带修复 gpu.py 死代码残迹

### v2.0.9

**修复**

- **large-v3-turbo 下载报 401 根治**：该模型的 CTranslate2 权重实际存放在社区仓库 `mobiuslabsgmbh/faster-whisper-large-v3-turbo`——Systran 官方下**不存在**此仓库（HF 对不存在仓库返回 401），此下拉项自引入起就无法下载（用户实测暴露）。现统一仓库 ID 解析：下载、缓存目录、管线加载三处全部跟随真实仓库
- 模型下载强制匿名（`token=False`）：本机存有过期/无效 Hugging Face 令牌时，公开仓库也会被附带的坏令牌拖累报 401——匿名即可，不再受本机残留令牌影响
- 401/403 加入友好错误映射（含"检查/删除本机 HF 令牌"的行动建议）

### v2.0.8

**修复**

- **"翻译没反应/一直显示正在聆听"根治**（用户实测 + 实时日志/电平探针联合定位）：识别链路五级排查（设备电平→采集线程→切句器→模型速度→队列）证明采集/切句/模型速度全部健康，真因是**识别积压丢段**——旧逻辑队列容量 3，medium CPU 每段转写 ~10 秒，快语速内容 30 秒切出 5+ 段时大多数段被静默丢弃。现队列容量扩到 8，只在真实超限时丢最旧；积压提示只报一次不再刷屏，且**常驻追加**到状态栏（此前一句话即被"识别完成"覆盖，用户从未看到丢段原因）
- **质量过滤不再静默**：幻觉过滤连续丢弃语音段时给出明确提示（"可能为音乐/噪声，或识别语言与内容不符"）——锁定识别语言播放其他语言内容是零字幕的高频成因

**改进**

- 积压/丢段事件落日志（`asr.segment_dropped`），报障可直接定位

### v2.0.7

**修复**

- **离线语言包重复下载**：已安装的方向不再出现在下载列表（此前仅标注"（已安装）"仍可重复下载，实测同一语言包 22 秒内被安装两次）；点击下载时与下载线程内双重兜底校验
- **停止翻译偶发崩溃**：已清理的孤儿线程上调用状态查询会抛 "Internal C++ object already deleted"，从停止流程中途炸出导致后续清理中断——已在孤儿容器巡查中捕获跳过
- **每次停止都孤儿化识别线程**：模型就绪后的预热空跑不可中断，1 秒的短会话停止也要等它跑完——预热结果标记在模型实例上只跑一次，且停止后不再启动

**改进**

- **"一直显示正在聆听"可诊断**：管线运行 30 秒仍零字幕时，状态栏给出明确指引（确认设备在播放声音/音量条有波动/未静音/换设备），并落日志；输入信号过弱事件同步落日志，便于报障定位

### v2.0.6

**修复**

- **存储迁移不再删目标数据**：迁移目标位置已存在同名内容（如误选了一个已有 `hf/` 的目录）时，此前会先静默删除目标再搬入——现改为拒绝迁移并提示换目录，杜绝数据丢失
- **音频设备按名匹配**：设备以名称（而非易漂移的索引）为主键——USB 声卡/蓝牙耳机热插拔导致索引变化后，仍能正确找回原设备，不再静默抓错音频源
- **限流误判**：错误分类中裸文本 "429" 改为词边界匹配，含 429 的路径/模型名不再被误判为限流
- 翻译缓存改攒批落盘（10 条或 5 秒合并写），实时字幕场景磁盘写入降一个量级

**改进**

- **主引擎自动恢复**：此前翻译备援成功后会话级钉死备援引擎；现队列空闲时每 60 秒静默重探主引擎，恢复即自动切回
- **重采样抗混叠**：48k/44.1k→16k 增加汉宁窗 sinc FIR 低通（截止≈7.6kHz），高频分量不再混叠进语音带拉低识别率（附信号级单测验证）
- **模型实例复用**：同一模型/设备组合在进程内缓存（容量 1）——切换输入来源、修改识别设置后重启管线不再全量重载模型（CPU 上原需数秒到数十秒）；删除模型时同步逐出缓存
- **设置项声明式登记**：三类维护清单（管线键/悬浮条键/暂存顺序）合并为单一登记表派生，"恢复默认漏键"类 bug 从结构上消除

### v2.0.5

**新增**

- **模型管理详情弹窗**：管理列表**双击模型**即可查看模型介绍、当前状态（未下载/下载不完整/已下载+体积），并可直接**下载**（带进度条与实时状态，逐文件可**取消下载**，断点续传——已下载部分保留，下次继续）或**删除**
- **手动下载模型**：首次使用前可在管理界面提前下载模型；管线启动时的自动下载同样升级为受控逐文件下载——加载期停止不再等待下载完成
- 下载不完整（残留半截文件）的模型在列表中如实标注「下载不完整 · 残留 N MB」，可删除或续传

**修复**

- **未下载的模型也显示"删除所选模型"**（按钮可点但删无可删）：删除按钮现在仅在所选模型本地确有数据时才可用
- `remove_model` 在 Windows 文件句柄占用下可能"半删却报成功"：重试 + 复核目录确实消失

### v2.0.4

**修复**

- **热键"关不掉"根治**（三处叠加缺陷，实机日志定位）：① 停止时对正在加载模型的识别线程白等 3 秒——该阶段线程阻塞在模型构造里无法响应停止，白冻界面且让连按的热键排队，恢复后被逐条当作新开关反向拉起管线；② 状态栏/音量条/管线错误回调缺"停止后丢弃"守卫——孤儿加载线程迟到的「正在加载模型」消息在写完"已停止"后覆盖，状态栏永久卡死在过期文案（用户误以为翻译没关掉）；③ 热键连按无防抖。现加载期停止 0.5s 内响应 + 全部回调加幽灵守卫 + 热键去抖
- **设置页"重新运行首次向导"必崩**：传参误传设置对话框自身（无 config / 启停管线方法），向导构建模型页时即崩溃——改传主窗口
- **翻译备援缓存只写不读**：备援成功后的缓存 key 缺源语言维度，与读取侧永不匹配（v2.0.1 加维度时漏改），白白占用 800 条缓存容量——读写统一 key 构造
- **日志双轨分裂**：启用自定义存储根后，生命周期日志写旧默认根、崩溃日志写新根（v2.0.1 只修了一半）——存储根迁移时统一改绑到新根

### v2.0.3

**修复**

- **孤儿线程根治**：停止超时后不再裸丢线程引用（防退出时 qFatal 崩溃）——摘除 parent 挂模块级容器保引用，finished 后自清理；进程退出前对仍存活的线程 terminate 兜底；切换输入来源改为等旧线程真正退出再重启（此前新旧采集并存抢音频设备、双份模型并发加载）
- **向导三连修**：模型列表补上 large-v3-turbo（此前已选该模型时向导会静默降级成 small）；仅在默认设备时才重置 device_index（重跑向导不再覆盖已选设备）；翻译运行中完成向导会自动重启管线让新配置生效
- **detect_lang 失败不再静默回 "en"**（此前非英文文本被按英文方向翻译出乱译并写入持久缓存）
- **手动代理忘填地址时显式直连**（此前静默跟随系统代理）
- Argos 索引缓存 / 系统代理缓存检查-写入原子化；超长无标点段字符级硬切；语言包顶层结构校验（防"解压出空目录仍报安装成功"）；metadata 写失败时清理残留目录

### v2.0.2

**新增**

- **通道全灭升级提示**：备援链全部失败（Google 全通道被封 + MyMemory 配额尽 + 无对应离线包）时，连续 3 条失败后状态栏与悬浮条升级为橙红常驻提示「翻译连续失败 N 条 · 检查网络/代理节点，或到设置-翻译测试通道 / 下载离线语言包」，直到出现成功译文才恢复——不再只让每条字幕各自静默报错

### v2.0.1 · 深度排雷补丁

> 四路并行代码审计（音频/识别、翻译/网络、UI、配置持久化）+ 汇总验证后的修复批次，共处理约 30 项独立发现。

**安全**

- **zip-slip 反斜杠绕过**：语言包解压的路径校验用 PurePosixPath（不认 `\`），而 Windows 拼接路径时把 `\` 当分隔符——恶意语言包可用 `root\..\..\evil` 绕过检测实现任意路径写文件。现统一归一校验 + realpath 前缀断言，并新增攻击样本回归测试

**生命周期 / 线程**

- **退出路径整体失效**：closeEvent 内重入 self.close() 被 Qt 抑制，"退出程序"后管线继续跑、托盘退出失灵。现抽 `_teardown()` 统一清理
- **幽灵回调守卫**：停止后仍会收到已入队的翻译结果/静音/错误信号（新增卡片、状态错乱）；三类回调加 running 守卫，信号 detach 清单补 muted
- **音频采集不再无限阻塞**：WASAPI loopback 静默期 stream.read 无限等待导致停止失效，改为轮询式读取（stop 10ms 内响应）；退出前 flush 补发"说完立刻停丢最后一句"
- **识别转写循环加协作取消点**：14s 段 CPU 大模型下转写可达数十秒且不可中断
- **模型加载阶段全兜底**：import/端点探测异常此前让线程静默死亡（UI 永远"正在加载"）

**正确性**

- **存储迁移三连修**：目标盘空间预检 + 失败回滚（此前半途失败"指针指旧根、数据在新根"，几 GB 模型被判未缓存重下）；迁移期间锁死 UI 入口（此前可并发二次迁移/启动翻译）；`relocate` 回写启动指针文件（此前迁移重启后失效）
- **配置持久化加固**：损坏 config.json 改名留存而非无声重置；save 加 fsync；翻译缓存改原子写 + 锁内保存 + 懒加载（自定义存储根的缓存此前会被默认根数据覆写）；relocate 后单例缓存不再读错位置
- **"恢复默认"补齐 4 个漏暂存的键**（幻觉抑制 / Silero VAD / 列表模式及条数——此前 UI 显示已恢复、保存后永不落盘）
- **悬浮字幕开关预览不再绕过暂存落盘**；"取消"现在真正还原样式预览
- **Google 备援链修复**：clients5 多行响应不再截断；中文源正确落 Argos 备援（此前 zh→zh-CN 归一缺失永不匹配）；MyMemory 拒收警告串（此前"MYMEMORY WARNING"被当译文上屏并污染缓存）；zh-TW 不再被静默降级简体
- **识别引擎启动失败有报错**（此前线程静默死亡）；识别积压丢段有提示；停止时补发最后一句字幕
- **"系统声音"模式尊重用户选定的输出设备**（此前被静默忽略）；设备索引 0 不再被吞成"默认设备"；回环解析失败不乱抓第一个设备
- **GPU 检测挪出 GUI 线程**（此前驱动异常时界面冻结 8 秒+）；设备下拉改用索引去重；悬浮条副屏放置修复；误听词典防抖与保存竞态修复；列表模式 QSS 双大括号修复；`apply_style` 不再冲掉 X 按钮样式
- 单测 22 → **26** 项（含 zip-slip 攻击样本、多行响应解析、分块零丢字）

### v2.0.0 · 可靠性与工程质量大版本

> 本版本源自真实用户实测（v1.9.0-v1.9.5 连环实测反馈） + 全代码审计的 12 项系统性改进。

**可靠性**

- **损坏缓存识别**：模型缓存判定从"文件存在"升级为"model.bin 体积 >50MB"——0 字节/半截文件不再被当完整缓存（v1.9.0 用户 NO SUCH FILE 事故根因），损坏时自动走重新下载
- **卸载语言包崩溃修复收尾**：缓存清理变量作用域问题彻底修复，重复卸载安全返回
- **MyMemory 按句边界分块**：不再把句子拦腰切成 480 字符碎片，翻译质量提升

**诊断能力**

- **结构化日志**：新增 UTF-8 滚动日志（1MB×2），管线启停、模型加载耗时、引擎选择/切换、代理出口、热键注册、语言包装卸全部落盘——用户报障看一眼日志即可定位
- **友好错误映射**：原始英文异常（ONNXRuntime NO SUCH FILE、超时、代理断开…）统一翻译为「中文结论 + 下一步动作」，识别/翻译/管线错误全链路接入
- **Google 四层备援链**：`dict-chrome-ex(googleapis)` → `gtx` → `clients5.google.com` → MyMemory → Argos（已装对应方向离线包时），抗 Google 单方面变脸
- **热键注册失败显式告警**：组合被占用/不支持时启动即弹托盘警告，不再静默失效

**工程化**

- **版本号一键同步**：新增 `scripts/bump_version.py`（含 CI 校验模式），版本一致性检查从 1 个文件扩展到 3 个（setup.iss / config.py / version_info.txt）
- **CI 提效**：main 分支推送只跑测试不打包（tag 才出包）；Whisper 模型加入 actions/cache，测试提速
- **硬编码收敛**：翻译 HTTP 头统一到 net.py（此前两处 UA 不一致）；Argos 下载候选源列表从目标语言派生，新增语言不再漏改
- **代理健壮性**：系统代理读取加 3 秒 TTL 缓存；仅 SOCKS 代理场景显式提示改「手动指定」（此前静默直连）
- **测试扩容**：单测 12 → 19 项，覆盖代理三模式、SOCKS 解析、损坏缓存判定、语言包卸载、友好错误映射、分块正确性、版本一致性

**界面**

- 设置-通用新增「重新运行首次向导」入口
- medium/large 模型在纯 CPU 上的实时性提示更明确

### v1.9.5

**改进**

- 语言支持描述与实际对齐（用户要求）：在线翻译引擎支持 16 种目标语言（全覆盖）；离线语言包（Argos 社区模型）实测支持其中 **15 种**目标方向（以英语→目标为主，**暂缺繁体中文**）。README 介绍、核心特性、功能列表与设置页文案四处同步更正，不再笼统写"16 种"

### v1.9.4

**修复**

- **卸载语言包报「删除目录时出错：cannot access local variable 'key'…」**：清理翻译器缓存时引用了循环外的未绑定变量（目录其实已删除，但弹了失败框、缓存清理被跳过）。现修正清理逻辑
- **Google 免费翻译整体 429 不可用**（用户实测发现）：2026-09 起 `client=gtx` 被 Google 大面积限流（与出口 IP 无关），换任何节点都连不通。现切换 `client=dict-chrome-ex`（同端点同响应结构，实测 0.2s 可用），翻译质量不受影响
- **热键启停「无响应/停不掉」**：窗口隐藏在托盘时启停全在后台静默发生，无任何可见反馈；停止时界面线程最长阻塞 15 秒。现改为启停即时弹系统托盘气泡（仅窗口隐藏时）+ 停止等待缩短到 3 秒（信号已断开，线程后台自行退出）

**改进**

- 设置-翻译「测试 Google 通道」的诊断细化：明确区分「代理不通 / 连接超时 / 出口 IP 被 Google 限流(429)」并给出对应建议；「手动代理」输入框占位符自动回显检测到的系统代理地址

### v1.9.3

**修复**

- **设置窗口所有弹窗「黑底黑字」**（用户实测发现）：`SETTING_QSS` 只给对话框设了深色背景，没有全局文字颜色规则，QMessageBox 文字仍是系统默认黑色——卸载语言包、删除模型、存储迁移、安装语言包等确认框全部不可读。现补齐基础文字/背景规则 + QMessageBox 专属样式（含按钮），并同步补上设置页 QComboBox 的深色样式（此前为系统默认浅色，与整体主题不符）

### v1.9.2

**修复**

- **识别模型下拉少一项**：下拉框是手写的 4 项硬编码列表，`large-v3-turbo` 没进下拉（模型管理页正常）。现统一改用 `MODELS` 常量，后续新增模型不会再漏
- **模型加载状态文案误导**：旧文案固定为「正在加载语音识别模型（首次运行会自动下载）」，已缓存模型离线加载 medium（1.5GB）时 CPU 需十几秒，用户误以为卡死。现文案带模型名与耗时预期（「正在加载 medium 模型（本地缓存，CPU 上通常需几秒到几十秒）」）

**改进**

- 加载期间按停止：模型加载完成后直接退出线程，不再短暂出现「就绪」假状态

### v1.9.1

**修复**

- **安装包漏打 Silero VAD 组件**：打包版开启「Silero VAD」后，识别时报 `ONNXRuntime … NO SUCH FILE: … faster_whisper\assets\silero_vad.onnx`（悬浮条显示一长串英文报错）。现已把 `faster_whisper/assets` 随包分发；代码侧同时兜底：资产缺失时自动回退默认切句并在状态栏中文提示，识别不再中断
- **热键启动可能长时间卡在「正在加载语音识别模型」**：模型缓存完整时此前仍会联网校验（代理抖动时表现为假死）。现改为缓存完整即离线加载（`local_files_only`，实测秒开），仅在确实需要下载时才走网络

### v1.9.0

**新增**

- **网络代理支持**：「跟随系统 / 手动 / 直连」三模式，读取 Windows 系统代理（v2rayN / Clash 开系统代理即用），解锁 Google 免费翻译通道；设置-翻译页可一键测试连通性
- **全局热键**：默认 `Ctrl+Alt+S` 任意界面开始/停止翻译，可改键（设置-通用），托盘菜单同步显示
- **首次运行向导**：三步引导选音频源 → 选模型 → 完成
- **悬浮字幕条信息栏**：顶部状态行（运行状态 · 输入来源 · 引擎 · 模型），翻译失败变橙红提醒；右上角 X 关闭；右键菜单重做（打开设置 / 切换输入来源 / 隐藏）
- **悬浮条紧凑列表模式**：可选显示最近 2-10 条字幕滚动回看
- **「版本与更新」设置页**：软件更新检查（GitHub Releases）、模型/语言包检查、更新日志与仓库链接
- **存储位置自定义**：模型/语言包/缓存一键迁移到任意盘，显示占用与磁盘余量
- **模型与语言包管理**：按体积查看、单独删除/卸载，正在使用的模型拦截保护
- **GPU / CUDA 检测与引导**：显卡/驱动/CUDA 环境检测 + 配置教程 + 源码模式一键安装 CUDA 版 PyTorch
- **识别质量套件**：幻觉抑制（过滤音乐/噪声段胡言）、Silero VAD 段内净化、误听修正词典、新增 large-v3-turbo 模型

**改进**

- 自适应切句：按语速动态调整判停 0.3-0.6s，快语速更跟手
- 翻译降级链对称化：Google ↔ MyMemory 双向互备，限流自动自愈
- 设置改「保存并应用」模式：改动暂存、底部操作栏、分层生效（管线自动重启），悬浮样式实时预览
- 设置 UI：导航图标、搜索框定位设置项
- QSlider 点击轨道直接定位；悬浮条拖动落点偏差根治
- 安装向导全面简体中文；CI 推 tag 自动发布 Release；新增 MIT LICENSE

### v1.8.1

**修复**

- **「背景透明度」滑条现在真正生效**：此前悬浮字幕背景由样式表渲染，在打包环境下更新透明度不会触发重绘——设置保存成功但视觉永远不变（该问题自悬浮样式功能上线即存在）。现改为 paintEvent 自绘背景，透明度/背景颜色实时生效
- 设置窗口新增「✓ 设置已自动保存」指示条：本产品设置即改即生效、没有确定按钮，每次改动后底部会短暂显示保存状态，不再让人怀疑"到底存没存"

### v1.8.0

**新增 / 改进**

- **模型下载进度可见**：首次使用时状态栏实时显示「正在下载识别模型（x/480MB，xx%）」，不再是一句黑盒文案
- **长字幕自动切分**：快语速内容（新闻/辩论/播客）的单条长识别结果，现在会按句末标点自动二次切分，字幕节奏更跟手
- **字幕导出默认保存到「文档」**：不再落在需要管理员权限的安装目录
- **设备列表净化**：麦克风列表过滤 MME 虚拟映射项（Sound Mapper / 主声音捕获），同名设备按规范化名称去重
- 悬浮字幕条拖动释放后立即+延迟双写位置，降低记录坐标与实际位置的偏差
- 「离线语言包」分组标题随翻译引擎切换整体显隐

### v1.7.0

**修复**

- 修复「关闭时询问」模式下，点击任意按钮（包括取消）都会误关主窗口的问题
- 修复麦克风模式下沿用系统声音的回环设备导致采集失败（Errno -9996）的问题：切换音频来源现在会自动重置设备，麦克风列表也不再混入回环设备
- 新增单实例互斥：重复启动会提示使用托盘图标，不再出现双进程写坏配置
- 音频采集失败后状态栏不再残留"就绪，正在聆听..."的假状态；错误提示中文化并附下一步指引
- 配置文件改为原子写入，异常断电不再导致设置静默丢失；兼容带 BOM 的手改配置
- 离线语言包下载/解压失败不再残留 .tmp 临时文件，启动设置时自动清理历史残留

**新增 / 改进**

- 状态栏新增「导出」按钮：一键把当前会话的双语字幕导出为文本文件
- 输入信号持续过弱（系统音量过低/抓错设备）时，状态栏主动提醒，字幕不再"静默罢工"
- 连续丢弃不确定语音段时，主动建议切换更大模型或锁定识别语言
- 悬浮字幕条拖动后立即记忆位置，且位置被限制在屏幕范围内（换分辨率不再找丢）
- 右键关闭悬浮字幕时，设置窗口的开关同步取消勾选
- 语言包下载区随翻译引擎切换整体显示/隐藏

### v1.6.0 及更早

见 [Releases](https://github.com/2465251326-netizen/live-subtitle/releases)。

---

**核心特性一览**

- 抓系统声音：网页视频、本地播放器、会议软件的声音直接采集，无需声卡设置（WASAPI loopback）
- 本地识别：faster-whisper 语音识别在本地运行，语音不上传服务器
- 自动语言检测：开启「自动检测」后，第一句话自动锁定说话语言，换语言视频无感切换
- 多语翻译：在线通道（免 Key、免注册）16 种目标语言；离线语言包（断网可用）覆盖其中 15 种（暂缺繁体中文）
- 悬浮字幕：置顶、可拖动、字号 / 颜色 / 背景透明度 / 描边全部自定义
- 系统托盘：关闭主窗口可隐藏到托盘，字幕在后台继续工作
- 免费无门槛：所有翻译通道均无需申请任何 API Key

---
