# 用户体验审查报告 R7：UI 全量走查（offscreen 截图 + 交互实测）

> 归档时间：2026-09-14 · 审查人：AI Coding Agent（全面真实使用应用的任务）
> 版本：v2.6.4（main `c318ce7`，源码运行）· 复审：v2.6.5 修复批次
> 方法：`QT_QPA_PLATFORM=offscreen` 驱动真实 UI 逐状态截图（21 张，`/tmp/opencode/shots/`），
> 交互代码走查 + 真实点击（导出/清空/开始翻译/设置搜索/错误反馈流）。
> 范围：主窗口、悬浮字幕条、设置 6 页、首次向导 3 步、错误路径。
> 沙箱限制：无音频设备、无真实语音，端到端转写用 mock 字幕流驱动；emoji 在截图中显示为 tofu（真实 Windows 上正常）。
>
> **修复状态（v2.6.5 批次）**：M1-M5、L3 已修复并重新截图验证；L2 复审撤销
> （空态卡本就有"点击右上角「开始翻译」"指引 + 配置速览卡，截图
> `01_main_initial.png` 证实）；L1/L4-L7 涉及产品取舍，暂缓。

## 一、发现的问题（按严重度）

### 中等：影响理解或操作效率

| # | 问题 | 证据 | 建议 |
|---|------|------|------|
| M1 | 悬浮条工具条 9 个按钮中 5 个无 tooltip：语言选择、原文/译文切换、Aa 字号、⋯ 菜单、收起、✕ 都没有悬停提示（仅"清空"和📌有）。符号按钮全靠猜 | `caption_overlay.py:119-177`（只有 150、164 两处 setToolTip） | 给全部按钮补 tooltip，尤其符号类（📌 ⋯ ✕ Aa） |
| M2 | 全站开关行布局：checkbox 与行标题分离，浮在说明文字右上角。显示页 2 个开关、翻译页"词典整词匹配"、通用页"启动后自动开始翻译"全部如此，checkbox 看起来像装饰点，从属关系模糊 | 截图 `real_3_显示.png`、`real_2_翻译.png`、`real_4_通用.png` | checkbox 移到标题左侧或右侧同一行，与标题保持水平对齐 |
| M3 | 设置页 footer 遮挡滚动内容：内容从 footer 分隔线下穿过，版本页"离线语言包"说明文字被遮一半仍可滚动 | 截图 `real_5_版本与更新.png` 底部 | footer 加不透明背景 + 滚动区底部留白（contentsMargins bottom） |
| M4 | 主窗状态点（左下红/黄/绿）无 tooltip，含义全靠旁边文字；文字被省略号截断时点的含义不可知 | `caption_overlay.py:360` `status_lbl.setToolTip("")`；实测 tooltip 为空串 | 按状态设置 tooltip（运行中/待启动/错误） |
| M5 | 设置页右侧滚动条压着内容边缘，下拉框右缘与滚动条重叠 | 截图 `real_2_翻译.png` 右缘 | 滚动区右侧加 margin，或细化滚动条样式占位 |

### 低：打磨项

| # | 问题 | 建议 |
|---|------|------|
| L1 | 向导不问翻译目标语言（默认"简体中文"藏在 设置→翻译）；第 3 步无"完成并立即开始翻译"选项 | 第 2 步加目标语言选择（或第 3 步清单提示默认值可改）；加"完成并立即开始"主按钮 |
| L2 | 空态引导不足：空态卡有热键"（未生效）"弹窗指引（`main_window.py:839-855`），但无"第一步点这里"式指引；悬浮条空态无"去主窗点开始翻译"引导 | 空态卡首行加一句"点击上方「开始翻译」或播放视频即可" |
| L3 | 无内容时"导出/清空"按钮可点，点了弹窗说"还没有可导出的字幕"（`main_window.py:1007-1009`） | 无内容时禁用两按钮（有字幕再 enable），或点击后状态栏内联提示替代弹窗 |
| L4 | 说明文案密度高：翻译页引擎说明 3-4 行长文塞满术语（回落/直译腔/通顺度） | 拆短句或收进"详情"折叠 |
| L5 | 卡片 meta"引擎: google"对非技术用户冗余 | 移到右键详情或设置页诊断区 |
| L6 | 字号概念双轨：设置→显示"字号"（12-48，行字号）与悬浮条 Aa 菜单（面板缩放）并行，易混淆 | Aa 菜单标题加副注"面板整体缩放"，或统一入口 |
| L7 | 两处"清空"同名不同义：悬浮条"清空"只清面板行，主窗"清空"清全部（含历史与导出源）。tooltip 有说明但按钮同名 | 悬浮条按钮改"清面板"或图标化+tooltip |

## 二、实测通过的部分（保持现状）

- 错误反馈流完整：无音频库时点"开始翻译"→ 状态栏清晰报错（"错误：缺少音频库 pyaudiowpatch…"）+ "音频错误"弹窗 + 按钮回弹"开始翻译"，running 正确复位 False。
- 设置搜索命中良好："字号"→1 项、"热键"→5 项、"模型"→6 项、乱词→"没有匹配的设置项"。
- 设置回显正确（真实路径 `load_from_config()`）：字号 18、模型 idx、透明度 92 全对；footer 状态提示（"所有改动已保存"/"有未保存的修改"）工作正常。
- 导出默认落文档目录、SRT 过滤器与入口一致；"清空"无内容时静默安全。
- 向导第 2 步模型分档说明实用（大小/延迟/适用场景），含硬件自动推荐；"重新运行首次向导"明确不改现有配置。
- 音频页预告"主窗口底部会显示实时音量条"；版本页图标+版本号展示得体，三个检查入口清晰。

## 三、复现方式

```bash
# 截图审查（驱动脚本）
QT_QPA_PLATFORM=offscreen python3 -u /tmp/opencode/ux_review.py
# 交互反馈流（导出/清空/开始翻译错误路径）
QT_QPA_PLATFORM=offscreen python3 -u /tmp/opencode/ux_review3.py
# 注意：向导 modal 会挂脚本，需 MainWindow._wizard_scheduled = True 拦截；
# 导出无内容弹 QMessageBox.information 静态方法，offscreen 下无法 patch，脚本内跳过点击
```

截图目录：`/tmp/opencode/shots/`（01 主窗、02 悬浮条、03-04 设置 6 页、05 向导 3 步、06 运行态、07 错误态、real_0-5 真实配置路径）。
